# MESH AI助教微信小程序 · 接口设计说明书

**文档编号：** API-DESIGN-V1.0
**版本：** v1.0.0
**日期：** 2026-08-30
**协议：** HTTPS / RESTful
**关联文档：**
- `04_整体架构设计方案.md`
- `05_数据库设计说明书.md`
- `../1.参考文档/02_系统交互协议规范.md`

---

## 目录

1. [通用规范](#1-通用规范)
2. [认证与账户模块](#2-认证与账户模块)
3. [五力认知测试模块](#3-五力认知测试模块)
4. [知识点导航模块](#4-知识点导航模块)
5. [个性化AI训练模块](#5-个性化ai训练模块)
6. [错题集模块](#6-错题集模块)
7. [智能助教模块](#7-智能助教模块)
8. [用户画像模块](#8-用户画像模块)
9. [管理后台模块](#9-管理后台模块)
10. [数据库设计勘误回填](#10-数据库设计勘误回填)

---

## 1. 通用规范

### 1.1 基础信息

| 项目 | 说明 |
|------|------|
| Base URL（小程序） | `https://api.mesh-edu.com/api/v1` |
| Base URL（管理后台） | `https://api.mesh-edu.com/api/v1/admin` |
| 协议 | HTTPS |
| 数据格式 | `Content-Type: application/json; charset=UTF-8` |
| 字符集 | UTF-8 |
| 时间格式 | ISO 8601，`2026-08-30T10:00:00Z`（UTC） |

### 1.2 统一响应格式

所有接口的返回结构统一如下：

```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {}
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `status` | string | 固定三值：`success`（成功）/ `fail`（业务失败）/ `error`（系统错误） |
| `code` | integer | 0 = 成功；非0 = 错误码（见 §1.4） |
| `msg` | string | 面向用户的提示信息（中文） |
| `data` | object/array/null | 业务数据体；成功时包含实际数据，失败时为 null |

**分页响应结构（列表接口）：**

```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [],
    "total": 100,
    "has_more": true,
    "next_cursor": 80
  }
}
```

> 所有列表接口采用 **Keyset Pagination（游标分页）**，禁止使用 OFFSET 分页。

### 1.3 认证方式

```
Authorization: Bearer {access_token}
```

- **小程序端**：access_token 有效期 7 天，refresh_token 有效期 30 天，静默续期
- **管理后台**：access_token 有效期 8 小时，无 refresh_token，到期重新登录
- **Token 格式**：RS256 签名的 JWT

**无需 Token 的接口（白名单）：**
- `POST /api/v1/auth/wx-login`
- `POST /api/v1/auth/admin-login`
- `POST /api/v1/auth/sms/send`
- `POST /api/v1/user/reactivate`
- `GET /health`

**Token 黑名单中间件规范：**

所有受保护接口的认证流程：
1. 解析 `Authorization` Header 中的 `access_token`
2. 验证 JWT 签名和 `exp` 过期时间
3. 查询 Redis 黑名单：`GET blacklist:{jti}`（jti = JWT 唯一身份字段）
   - Key 存在 → 返回 `AUTH-001`（Token 已撤销）
   - Key 不存在 → 放行
4. 验证 `role` 字段权限后执行业务逻辑

**JWT Payload 规范：**
```json
{
  "jti": "uuid-唯一标识",
  "sub": "1001",
  "role": "STUDENT",
  "iat": 1693363200,
  "exp": 1693967200,
  "iss": "mesh-auth-service"
}
```

**Token 黑名单 Redis 存储：**
```
Key: blacklist:{jti}
Value: revoked
TTL: token 剩余有效期（自动过期，无需手动清理）
```

### 1.4 错误码规范

| 前缀 | 范围 | 模块 |
|------|------|------|
| `0` | — | 成功 |
| `AUTH-*` | 身份认证 | Token 过期/无效/权限不足 |
| `LOGIN-*` | 登录流程 | 密码错误/账号锁定 |
| `SMS-*` | 短信验证码 | 频率限制/验证码错误 |
| `REG-*` | 注册/绑定 | 手机号已注册/openid 已绑定 |
| `BIND-*` | 家长绑定 | 邀请码无效/绑定数量超限 |
| `TEST-*` | 五力测试 | 重测限制/画像生成 |
| `TRAIN-*` | 训练模块 | 无画像/题目不足 |
| `ASST-*` | 智能助教 | 轮数满/内容安全 |
| `ADMIN-*` | 管理后台 | 操作权限/配置错误 |
| `SYS-*` | 系统级 | AI 服务不可用/限流 |

**常用错误码：**

| 错误码 | HTTP状态 | 说明 | 用户提示 |
|--------|---------|------|---------|
| `AUTH-001` | 401 | Token 无效或已过期 | 请重新登录 |
| `AUTH-002` | 401 | Token 已在其他设备登录 | 账号已在其他设备登录 |
| `AUTH-003` | 403 | 权限不足 | 暂无权限 |
| `LOGIN-001` | 401 | 用户名/密码错误 | 账号或密码错误，剩余X次机会 |
| `LOGIN-002` | 403 | 账号已锁定 | 账号已锁定，请X分钟后重试 |
| `LOGIN-003` | 403 | 首次登录需改密 | 请先修改初始密码 |
| `SMS-001` | 429 | 发送太频繁 | 请60秒后再试 |
| `SMS-002` | 400 | 验证码错误 | 验证码错误，还可尝试X次 |
| `SMS-003` | 429 | 当日发送超限 | 今日验证码发送次数已达上限 |
| `REG-001` | 409 | 手机号已注册 | 该手机号已注册，请直接登录 |
| `REG-002` | 409 | openid 已绑定 | 当前微信已绑定账号 |
| `BIND-001` | 404 | 邀请码无效/已过期 | 邀请码不存在或已过期 |
| `BIND-002` | 409 | 绑定数量超限 | 最多绑定2位家长 |
| `TEST-001` | 409 | 重测间隔不足 | 距上次测试不足30天，暂不可重测 |
| `TRAIN-001` | 403 | 无五力画像 | 请先完成五力测试 |
| `TRAIN-002` | 400 | 知识点题目不足 | 该知识点题库建设中，暂不支持训练 |
| `ASST-001` | 400 | 对话轮数已满 | 本次对话已达上限，开启新对话继续？ |
| `ASST-002` | 400 | 内容安全拦截 | 该内容无法回复，换个方式试试？ |
| `SYS-001` | 503 | AI 服务暂不可用 | AI助手暂时休息，请稍后再试 |
| `SYS-002` | 429 | 接口限流 | 请求过于频繁，请稍后再试 |

---

## 2. 认证与账户模块

### 2.1 微信登录（学生/家长）

**接口：** `POST /api/v1/auth/wx-login`
**认证：** 无需 Token
**说明：** 微信授权，通过 code 换取 openid，判断是否已注册

**请求体：**
```json
{
  "code": "wx_auth_code_xxx",
  "user_info": {
    "nickname": "小明",
    "avatar_url": "https://..."
  }
}
```

**响应（新用户，需绑定手机号）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "授权成功，请绑定手机号",
  "data": {
    "is_new_user": true,
    "temp_token": "临时token，仅用于绑定手机号，有效期10分钟",
    "openid_hash": "用于前端临时存储，不含实际openid"
  }
}
```

**响应（已注册用户）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "登录成功",
  "data": {
    "is_new_user": false,
    "access_token": "eyJ...",
    "refresh_token": "eyJ...",
    "access_expires_in": 604800,
    "user_info": {
      "student_id": 1001,
      "nickname": "小明",
      "avatar_url": "https://...",
      "grade": "G9",
      "has_five_power_profile": true,
      "recommended_mode": "TRAIN_WEAKNESS"
    }
  }
}
```

**操作数据库：** 查询 `students`（by openid_hash），写入 `students`（新用户）

---

### 2.2 发送短信验证码

**接口：** `POST /api/v1/auth/sms/send`
**认证：** 无需 Token（新注册）或携带 temp_token
**说明：** 发送短信验证码，用于手机号绑定/登录/2FA

**请求体：**
```json
{
  "phone": "13800138000",
  "purpose": "BIND_PHONE"
}
```

`purpose` 可选值：`BIND_PHONE` / `LOGIN` / `TWO_FA`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "验证码已发送，5分钟内有效",
  "data": {
    "expire_seconds": 300,
    "cooldown_seconds": 60
  }
}
```

**业务约束：** 同号每60秒仅1次，每日最多10次
**操作数据库：** 写入 `sms_codes`

---

### 2.3 绑定手机号（新用户完成注册）

**接口：** `POST /api/v1/auth/bind-phone`
**认证：** `temp_token`（2.1接口返回）

**请求体：**
```json
{
  "phone": "13800138000",
  "sms_code": "123456",
  "grade": "G9",
  "semester": "S1",
  "subject_prefs": ["MATH", "PHYSICS"]
}
```

**字段说明：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `grade` | string | 是 | G7~G12 |
| `semester` | string | 是 | `S1`（上学期）/ `S2`（下学期）|
| `subject_prefs` | array | 是 | `MATH` / `PHYSICS` / `CHEMISTRY`，至少选1科 |

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "注册成功",
  "data": {
    "access_token": "eyJ...",
    "refresh_token": "eyJ...",
    "access_expires_in": 604800,
    "user_info": {
      "student_id": 1002,
      "nickname": "小明",
      "grade": "G9",
      "semester": "S1",
      "has_five_power_profile": false
    }
  }
}
```

**操作数据库：** 写入 `students`（phone/phone_hash/grade/semester/subject_prefs），查询 `sms_codes`

---

### 2.4 刷新 Token

**接口：** `POST /api/v1/auth/token/refresh`
**认证：** 携带 `refresh_token`（Header或Body均可）

**请求体：**
```json
{
  "refresh_token": "eyJ..."
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "Token刷新成功",
  "data": {
    "access_token": "eyJ...",
    "access_expires_in": 604800
  }
}
```

**操作数据库：** 写入 Redis（旧Token加入黑名单）

---

### 2.5 退出登录

**接口：** `POST /api/v1/auth/logout`
**认证：** 需要 Token

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "已退出登录",
  "data": null
}
```

**操作数据库：** 写入 Redis（Token加入黑名单）

---

### 2.6 生成家长邀请码

**接口：** `POST /api/v1/user/invite-code`
**认证：** 学生 Token

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "邀请码已生成",
  "data": {
    "code": "A3B7K9",
    "expire_at": "2026-09-01T10:00:00Z",
    "expire_hours": 48
  }
}
```

**操作数据库：** 写入 `invite_codes`（旧码自动作废），查询 `students`

---

### 2.7 家长绑定——方式一：使用邀请码（家长端）

> 学生生成邀请码后，家长在小程序输入邀请码完成绑定，立即生效。

**接口：** `POST /api/v1/user/parent-bind/invite-code`
**认证：** 家长 Token（`user_type='PARENT'`）

**请求体：**
```json
{
  "invite_code": "A3B7K9"
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "绑定成功",
  "data": {
    "binding_id": 101,
    "bind_method": "INVITE_CODE",
    "student_info": {
      "student_id": 1001,
      "nickname": "小明",
      "grade": "G9",
      "avatar_url": "https://..."
    }
  }
}
```

**操作数据库：** 写入 `parent_student_bindings`（`bind_method='INVITE_CODE'`，`bind_status='active'`），更新 `invite_codes.is_used = true`

---

### 2.7b 家长绑定——方式二：家长主动发起（家长端）

> 家长输入学生手机号，系统向学生发送确认短信，学生确认后绑定生效。绑定请求有效期 24 小时。

**接口：** `POST /api/v1/user/parent-bind/request`
**认证：** 家长 Token（`user_type='PARENT'`）

**请求体：**
```json
{
  "student_phone": "13800000000"
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "绑定申请已发送，等待学生确认",
  "data": {
    "binding_id": 102,
    "bind_status": "pending",
    "expires_at": "2026-09-13T10:00:00Z"
  }
}
```

**操作数据库：** 写 `parent_student_bindings`（`bind_method='PARENT_SELF'`，`bind_status='pending'`），发送短信通知至学生手机

---

### 2.7c 学生确认家长绑定申请（学生端）

> 学生收到短信通知后，在小程序中确认或拒绝家长的绑定请求。

**接口：** `POST /api/v1/user/parent-bind/confirm`
**认证：** 学生 Token（`user_type='STUDENT'`）

**请求体：**
```json
{
  "binding_id": 102,
  "action": "accept"
}
```

| `action` 值 | 说明 |
|------------|------|
| `accept` | 接受绑定，`bind_status` 变为 `active` |
| `reject` | 拒绝绑定，`bind_status` 变为 `rejected` |

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "已接受绑定",
  "data": {
    "binding_id": 102,
    "bind_status": "active"
  }
}
```

**操作数据库：** 更新 `parent_student_bindings.bind_status`，`accept` 时置 `confirmed_at=NOW()`

---

### 2.8 管理员登录（Step 1：账号密码）

**接口：** `POST /api/v1/admin/auth/login`
**认证：** 无需 Token

**请求体：**
```json
{
  "username": "admin001",
  "password": "plaintext_password"
}
```

**响应（需要2FA）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "密码验证成功，请完成短信验证",
  "data": {
    "two_fa_required": true,
    "phone_masked": "138****8000",
    "session_token": "2fa_session_xxx（仅用于Step2，有效期5分钟）"
  }
}
```

**操作数据库：** 查询 `admin_users`（bcrypt比对），更新 `login_fail_count`

---

### 2.9 管理员登录（Step 2：短信2FA）

**接口：** `POST /api/v1/admin/auth/verify-2fa`
**认证：** `session_token`（Step 1返回）

**请求体：**
```json
{
  "sms_code": "654321",
  "session_token": "2fa_session_xxx"
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "登录成功",
  "data": {
    "access_token": "eyJ...",
    "access_expires_in": 28800,
    "admin_info": {
      "admin_id": 1,
      "username": "admin001",
      "display_name": "系统管理员",
      "role": "SUPER_ADMIN",
      "password_expired": false
    }
  }
}
```

**操作数据库：** 查询 `sms_codes`，更新 `admin_users.last_login_at`，写入 `admin_audit_logs`

---

## 3. 五力认知测试模块

### 3.1 获取测试题卷

**接口：** `GET /api/v1/test/questions`
**认证：** 学生 Token
**说明：** 返回20道固定测试题（按 `display_order` 打散顺序），支持中断恢复

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "session_id": 10001,
    "is_resumed": false,
    "current_question_index": 0,
    "total_questions": 20,
    "questions": [
      {
        "question_id": 5,
        "display_order": 1,
        "question_type": "STANDARD",
        "stem": "有一个封闭房间，里面放着完全相同的三杯热水...",
        "image_url": null,
        "options": [
          { "key": "A", "text": "三个杯子的水最后都会变凉" },
          { "key": "B", "text": "B和C都加了盖，但结果仍然不同" },
          { "key": "C", "text": "A杯没有毛巾" },
          { "key": "D", "text": "实验时间都是20分钟" }
        ],
        "reference_time_sec": 90
      }
    ]
  }
}
```

**业务说明：**
- 若已有 `in_progress` 状态的会话，直接返回该会话及当前进度（`is_resumed: true`）
- `question_type` 前台不展示，用于后端计分路径判断
- 不返回 `option_scores` 和 `option_force_weights`（防止学生猜测测的是什么）

**操作数据库：** 读 `cognitive_test_questions`，读/写 `test_sessions`

---

### 3.2 提交单题答案

**接口：** `POST /api/v1/test/answer`
**认证：** 学生 Token

**请求体：**
```json
{
  "session_id": 10001,
  "question_id": 5,
  "question_index": 0,
  "selected_option": "B",
  "time_spent_sec": 45,
  "modify_count": 1
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "答案已记录",
  "data": {
    "answered_count": 1,
    "remaining_count": 19,
    "can_submit": false
  }
}
```

**业务说明：**
- 支持覆盖提交（学生修改答案），以最后一次提交为准
- `time_spent_sec < 5` 时，后端标记 `is_rushed = true`
- 不返回正确/错误，避免影响后续答题行为

**操作数据库：** 写/更新 `test_answer_records`，更新 `test_sessions.current_question_index`

---

### 3.3 完成测试（触发评分）

**接口：** `POST /api/v1/test/complete`
**认证：** 学生 Token

**请求体：**
```json
{
  "session_id": 10001
}
```

**响应（同步，≤3秒）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "测试完成，正在生成你的五力画像",
  "data": {
    "profile_id": 2001,
    "forces": {
      "insight":    { "ability": 82, "preference": 34, "final": 69, "level": "strong" },
      "structure":  { "ability": 47, "preference": 18, "final": 41, "level": "weak"   },
      "inference":  { "ability": 74, "preference": 17, "final": 57, "level": "good"   },
      "adaptation": { "ability": 55, "preference": 21, "final": 45, "level": "medium" },
      "transfer":   { "ability": 51, "preference": 10, "final": 39, "level": "weak"   }
    },
    "primary_weakness": "structure",
    "secondary_weakness": "transfer",
    "primary_strength": "insight",
    "primary_entry": "insight",
    "recommended_mode": "TRAIN_WEAKNESS",
    "preferred_force": "insight",
    "profile_confidence": 0.65,
    "ai_analysis_status": "pending",
    "ai_analysis_text": null
  }
}
```

**业务说明：**
- 同步返回三层评分结果（ability / preference / final）
- `ai_analysis_text` 由异步任务生成，前端轮询 `GET /api/v1/test/result/{id}` 获取最新状态
- 若 `is_latest` 已有记录，先将旧记录置 `false`

**操作数据库：**
- 更新 `test_sessions.session_status = 'completed'`
- 写入 `five_power_profiles`（`is_latest=true`），旧记录 `is_latest=false`
- 触发 Celery Queue-E：AI分析文字异步生成

---

### 3.4 查询测试结果（含AI分析轮询）

**接口：** `GET /api/v1/test/result/{profile_id}`
**认证：** 学生 Token

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "profile_id": 2001,
    "forces": { "...（同3.3结构）": "" },
    "ai_analysis_status": "completed",
    "ai_analysis_text": "你的洞察力非常突出，善于从复杂信息中抓到关键……",
    "recommended_mode": "TRAIN_WEAKNESS",
    "preferred_force": "insight",
    "created_at": "2026-08-30T10:00:00Z"
  }
}
```

**操作数据库：** 读 `five_power_profiles`

---

### 3.5 查询历史测试记录

**接口：** `GET /api/v1/test/history`
**认证：** 学生 Token

**Query Params：**
```
?limit=10&before_id=2000
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "profile_id": 2001,
        "is_latest": true,
        "forces": { "insight": { "ability": 82, "final": 69 } },
        "primary_weakness": "structure",
        "test_date": "2026-08-30T10:00:00Z"
      }
    ],
    "total": 3,
    "has_more": false,
    "next_cursor": null
  }
}
```

**操作数据库：** 读 `five_power_profiles`（by student_id, 按时间倒序）

---

### 3.6 五力成长对比

**接口：** `GET /api/v1/test/compare`
**认证：** 学生 Token

**Query Params：**
```
?profile_id_a=2001&profile_id_b=1998
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "profile_a": {
      "profile_id": 2001,
      "test_date": "2026-08-30T10:00:00Z",
      "forces": { "insight": { "ability": 82, "final": 69, "level": "strong" } }
    },
    "profile_b": {
      "profile_id": 1998,
      "test_date": "2026-07-01T10:00:00Z",
      "forces": { "insight": { "ability": 70, "final": 58, "level": "good" } }
    },
    "growth": {
      "insight": { "ability_delta": 12, "final_delta": 11, "trend": "up" },
      "structure": { "ability_delta": -3, "final_delta": -2, "trend": "down" }
    }
  }
}
```

**操作数据库：** 读 `five_power_profiles`（by profile_id_a / profile_id_b）

---

## 4. 知识点导航模块

### 4.1 获取学科列表

**接口：** `GET /api/v1/knowledge/subjects`
**认证：** 学生 Token

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [
      { "subject_id": 1, "code": "MATH", "name": "数学" },
      { "subject_id": 2, "code": "PHYSICS", "name": "物理" },
      { "subject_id": 3, "code": "CHEMISTRY", "name": "化学" }
    ]
  }
}
```

**操作数据库：** 读 `subjects`（is_active=true）

---

### 4.2 获取年级列表

**接口：** `GET /api/v1/knowledge/grades`
**认证：** 学生 Token

**Query Params：** `?subject_id=1`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [
      { "grade_id": 7, "code": "G9", "name": "初三（九年级）" },
      { "grade_id": 8, "code": "G10", "name": "高一" }
    ]
  }
}
```

**操作数据库：** 读 `grades`（by subject_id, is_active=true）

---

### 4.3 获取章节与知识点列表

**接口：** `GET /api/v1/knowledge/points`
**认证：** 学生 Token
**说明：** 返回某年级下所有章节及知识点（含可用状态），前端按三态展示

**Query Params：** `?grade_id=7`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "chapters": [
      {
        "chapter_id": 101,
        "name": "二元一次方程组",
        "knowledge_points": [
          {
            "kp_id": 1001,
            "code": "MATH-G9-CH03-KP007",
            "name": "二元一次方程组应用题",
            "available": true,
            "question_count": 12
          },
          {
            "kp_id": 1002,
            "code": "MATH-G9-CH03-KP008",
            "name": "二元一次方程组图形问题",
            "available": false,
            "question_count": 3
          }
        ]
      }
    ]
  }
}
```

**业务说明：**
- `available: true`：`question_count ≥ 5`，显示绿色可选
- `available: false`：`question_count 1~4`，显示灰色"建设中"
- `question_count = 0`：不返回该知识点（隐藏）

**操作数据库：** 读 `chapters`、`knowledge_points`，聚合查询 `questions` 计数

---

## 5. 个性化AI训练模块

### 5.1 创建训练会话（开始训练）

**接口：** `POST /api/v1/training/session`
**认证：** 学生 Token

**说明：** 支持**五种**训练维度（v2.0新增 ERROR_QUESTIONS）。系统读取该维度当前激活的 `training_dimension_configs` 作为本次会话参数。

**请求体（按知识点）：**
```json
{
  "training_dimension": "KNOWLEDGE_POINT",
  "knowledge_point_id": 1001
}
```

**请求体（按单元）：**
```json
{
  "training_dimension": "UNIT",
  "chapter_id": 12
}
```

**请求体（按学期）：**
```json
{
  "training_dimension": "SEMESTER",
  "semester_id": 5,
  "subject_code": "MATH"
}
```

**请求体（错题集）：**
```json
{
  "training_dimension": "ERROR_QUESTIONS",
  "subject_code": "MATH"
}
```

**请求体（随机）：**
```json
{
  "training_dimension": "RANDOM",
  "subject_code": "MATH"
}
```

**训练维度参数说明：**

| `training_dimension` | 必填参数 | 选题范围 |
|---------------------|---------|---------|
| `KNOWLEDGE_POINT` | `knowledge_point_id` | 指定知识点下的题目 |
| `UNIT` | `chapter_id` | 指定单元下所有知识点 |
| `SEMESTER` | `semester_id` + `subject_code` | 指定学期+学科下所有知识点 |
| `ERROR_QUESTIONS` | `subject_code` | 该科目的 wrong_answer_records（待攻克）|
| `RANDOM` | `subject_code` | 该科目所有已发布题目，跨知识点随机抽取 |

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "训练会话已创建",
  "data": {
    "session_id": "uuid-training-xxx",
    "training_dimension": "KNOWLEDGE_POINT",
    "training_mode": "TRAIN_WEAKNESS",
    "training_focus_power": "STRUCTURE",
    "preferred_force": "INSIGHT",
    "dimension_config": {
      "version_number": "v3",
      "questions_per_session": 5,
      "difficulty_ratio": {"basic": 50, "advanced": 35, "challenge": 15}
    },
    "scope_info": {
      "knowledge_point_id": 1001,
      "name": "二元一次方程组应用题"
    },
    "questions": [
      {
        "question_id": 5001,
        "question_index": 0,
        "stem": "几个人一起买一件东西...",
        "image_url": null,
        "difficulty": "basic",
        "power_type": "STRUCTURE"
      }
    ],
    "total_questions": 5,
    "tips": {
      "focus_power_name": "建构力",
      "entry_power_name": "洞察力",
      "hint": "AI会先从你擅长的洞察方式帮你进入建构力训练"
    }
  }
}
```

**业务说明：**
- 检查 `five_power_profiles.is_latest=true`，无则返回 `TRAIN-001`
- 按知识点/单元/学期维度时，范围内题目不足5道返回 `TRAIN-002`
- 错题集维度时，该科目待攻克错题数=0返回 `TRAIN-003`
- 系统读取该维度 `training_dimension_configs.is_active=true` 配置，写入 `dimension_config_id`
- 错题集维度：按 `wrong_priority` 排序，按 `new_question_mix_ratio` 混入新题

**错误码：**

| 错误码 | 说明 |
|--------|------|
| `TRAIN-001` | 未完成五力测试，无法训练 |
| `TRAIN-002` | 范围内题目不足5道 |
| `TRAIN-003` | 错题集为空，该科目暂无待攻克错题 |

**操作数据库：** 读 `five_power_profiles`、`training_dimension_configs`（当前激活版本）、`questions`；ERROR_QUESTIONS额外读 `wrong_answer_records`；写 `training_sessions`


### 5.2 提交单题答案

**接口：** `POST /api/v1/training/answer`
**认证：** 学生 Token

**请求体：**
```json
{
  "session_id": "uuid-training-xxx",
  "question_id": 5001,
  "question_index": 0,
  "student_answer": "400x - y = 3400",
  "time_spent_sec": 120,
  "hint_level_used": 0
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "提交成功",
  "data": {
    "answer_record_id": 80001,
    "is_correct": false,
    "rag_status": "pending",
    "proactive_trigger": false,
    "answered_count": 1,
    "remaining_count": 4,
    "can_complete": false
  }
}
```

**业务说明：**
- `is_correct` 同步返回判题结果
- 答错时：异步推送 Celery Queue-C（RAG生成），`rag_status = 'pending'`
- 连续答错≥2次时：检查5个前置条件，若满足则 `proactive_trigger = true`（前端据此展示悬浮条）

**操作数据库：**
- 写 `training_answer_records`
- 答错时写 `wrong_answer_records`（review_status='unreview'）
- 触发 Celery 异步任务

---

### 5.3 查询RAG生成状态（前端轮询）

**接口：** `GET /api/v1/training/rag-status/{answer_record_id}`
**认证：** 学生 Token
**说明：** 前端每3秒轮询，最多15次（45秒）

**响应（生成中）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "rag_status": "pending",
    "rag_content": null,
    "static_solution": null,
    "retry_count": 0
  }
}
```

**响应（完成）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "rag_status": "completed",
    "rag_content": "从建构力的角度看这道题...",
    "static_solution": null,
    "retry_count": 0
  }
}
```

**响应（降级）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "rag_status": "degraded",
    "rag_content": null,
    "static_solution": "参考解析：设人数为x，总价为y...",
    "retry_count": 1,
    "can_retry": true
  }
}
```

**操作数据库：** 读 `training_answer_records`（rag_status, rag_content）

---

### 5.4 重新生成RAG（降级后重试）

**接口：** `POST /api/v1/training/rag-retry`
**认证：** 学生 Token

**请求体：**
```json
{
  "answer_record_id": 80001
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "重新生成中",
  "data": {
    "rag_status": "pending",
    "retry_count": 2,
    "can_retry": true
  }
}
```

**业务约束：** 每题最多重试3次

**操作数据库：** 读 `training_answer_records`，触发 Celery Queue-C

---

### 5.5 完成训练会话

**接口：** `POST /api/v1/training/complete`
**认证：** 学生 Token

**请求体：**
```json
{
  "session_id": "uuid-training-xxx"
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "训练完成",
  "data": {
    "session_id": "uuid-training-xxx",
    "summary": {
      "total_questions": 5,
      "correct_count": 3,
      "accuracy_rate": 0.60,
      "time_spent_sec": 780,
      "power_breakdown": [
        { "power": "structure", "correct": 1, "total": 3, "accuracy": 0.33 },
        { "power": "insight",   "correct": 2, "total": 2, "accuracy": 1.00 }
      ]
    },
    "profile_update_status": "pending",
    "wrong_answer_count": 2
  }
}
```

**业务说明：**
- 允许提前结束（未全部作答），提前结束时小结显示实际进度
- 触发 Celery Queue-P：five_power_training_profiles 异步EWMA更新（≤5分钟）

**操作数据库：**
- 更新 `training_sessions`（session_status='completed', correct_count, ended_at）
- 触发 Celery Queue-P（训练画像更新）

---

### 5.6 创建微训练（04模块调度）

**接口：** `POST /api/v1/training/mini-session`
**认证：** 学生 Token

**请求体：**
```json
{
  "knowledge_point_id": 1001,
  "target_force": "structure",
  "question_count": 2,
  "source_chat_session_id": "uuid-chat-xxx"
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "微训练已创建",
  "data": {
    "session_id": "uuid-mini-training-xxx",
    "training_mode": "TRAIN_WEAKNESS",
    "questions": [
      {
        "question_id": 5002,
        "question_index": 0,
        "stem": "...",
        "question_type": "app",
        "difficulty": "basic"
      }
    ]
  }
}
```

**操作数据库：** 写 `training_sessions`（source_type='mini', parent_chat_session_id）

---

## 6. 错题集模块

### 6.1 获取错题列表

**接口：** `GET /api/v1/wrong-answers`
**认证：** 学生 Token

**Query Params：**
```
?review_status=unreview&limit=20&before_id=uuid-xxx
```

`review_status` 可选：`unreview` / `reviewing` / `pending_consolidation`（不传则返回全部未突破的）

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "wrong_id": "uuid-wrong-xxx",
        "question": {
          "question_id": 5001,
          "stem": "几个人一起买一件东西...",
          "difficulty": "basic",
          "primary_power": "structure",
          "knowledge_point_name": "二元一次方程组应用题"
        },
        "student_answer": "400x + 3400 = y",
        "review_status": "unreview",
        "created_at": "2026-08-30T09:00:00Z",
        "reviewed_at": null,
        "aha_at": null
      }
    ],
    "total": 15,
    "has_more": true,
    "next_cursor": "uuid-wrong-yyy"
  }
}
```

**操作数据库：** 读 `wrong_answer_records`（by student_id），关联读 `questions`

---

### 6.2 发起AI复盘

**接口：** `POST /api/v1/wrong-answers/{wrong_id}/review`
**认证：** 学生 Token
**说明：** 针对某道错题开启AI复盘对话会话

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "复盘对话已开启",
  "data": {
    "chat_session_id": "uuid-chat-xxx",
    "session_mode": "wrong_review",
    "wrong_id": "uuid-wrong-xxx",
    "question_info": {
      "stem": "几个人一起买一件东西...",
      "student_answer": "400x + 3400 = y",
      "primary_power": "structure"
    },
    "opening_message": {
      "message_id": 1001,
      "role": "assistant",
      "content": "我注意到你之前在这道建构力题目上遇到了困难。你现在愿意和我一起重新看看这道题吗？",
      "created_at": "2026-08-30T10:00:00Z"
    }
  }
}
```

**操作数据库：**
- 写 `ai_chat_sessions`（session_mode='wrong_review', linked_wrong_id）
- 更新 `wrong_answer_records.review_status = 'reviewing', reviewed_at`

---

## 7. 智能助教模块

### 7.1 创建对话会话

**接口：** `POST /api/v1/assistant/session`
**认证：** 学生 Token

**请求体：**
```json
{
  "session_mode": "free_chat",
  "linked_question_id": null,
  "linked_wrong_id": null
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "对话会话已创建",
  "data": {
    "chat_session_id": "uuid-chat-xxx",
    "session_mode": "free_chat",
    "opening_message": {
      "message_id": 1001,
      "role": "assistant",
      "content": "你好！我是你的AI学习伙伴。有什么数理化问题想聊聊？",
      "created_at": "2026-08-30T10:00:00Z"
    }
  }
}
```

**业务约束：** 同一学生最多1个 active 会话，新建时关闭旧会话（status='completed'）

**操作数据库：** 写 `ai_chat_sessions`，更新旧会话状态，写 `ai_chat_messages`

---

### 7.2 发送消息

**接口：** `POST /api/v1/assistant/message`
**认证：** 学生 Token

**请求体：**
```json
{
  "session_id": "uuid-chat-xxx",
  "content": "为什么二次函数的顶点公式是这样的？",
  "image_url": null
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "user_message": {
      "message_id": 1002,
      "role": "student",
      "content": "为什么二次函数的顶点公式是这样的？",
      "created_at": "2026-08-30T10:01:00Z"
    },
    "ai_reply": {
      "message_id": 1003,
      "role": "assistant",
      "content": "你能从这道题中找出哪些信息在变化吗？",
      "strategy_id": "S-SOCRATIC",
      "is_proactive": false,
      "tokens_used": 380,
      "created_at": "2026-08-30T10:01:02Z"
    },
    "session_stats": {
      "current_round": 2,
      "max_rounds": 30,
      "proactive_count": 0,
      "silence_mode": false
    }
  }
}
```

**业务说明：**
- `content` 最大 500 字，超出返回 `ASST-003`
- AI 回复首字延迟 ≤3s（P95）
- AI 回复最大 400 字、≤800 Token
- 超过30轮对话返回 `ASST-001`，提示开启新对话

**操作数据库：** 写 `ai_chat_messages`（x2：学生消息+AI回复），读 `ai_student_strategy_profiles`、`wrong_answer_records`、`five_power_profiles`

---

### 7.3 获取历史消息

**接口：** `GET /api/v1/assistant/messages`
**认证：** 学生 Token

**Query Params：** `?session_id=uuid-chat-xxx&limit=20&before_id=1002`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "message_id": 1001,
        "role": "assistant",
        "content": "你好！我是你的AI学习伙伴。",
        "strategy_id": null,
        "is_proactive": false,
        "student_rating": null,
        "created_at": "2026-08-30T10:00:00Z"
      }
    ],
    "has_more": false,
    "next_cursor": null
  }
}
```

**操作数据库：** 读 `ai_chat_messages`（by session_id，时间倒序）

---

### 7.4 对AI消息评分（点赞/踩）

**接口：** `PATCH /api/v1/assistant/message/{message_id}/rating`
**认证：** 学生 Token

**请求体：**
```json
{
  "rating": "like"
}
```

`rating` 可选：`like` / `dislike` / `cancel`（取消评分）

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "评价已记录",
  "data": {
    "message_id": 1003,
    "student_rating": "like"
  }
}
```

**操作数据库：** 更新 `ai_chat_messages.student_rating`，异步更新 `ai_student_strategy_profiles`

---

### 7.5 结束对话会话

**接口：** `POST /api/v1/assistant/session/{session_id}/end`
**认证：** 学生 Token

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "对话已结束",
  "data": {
    "session_id": "uuid-chat-xxx",
    "summary": "本次对话中，你在建构力方面产生了一次理解突破...",
    "aha_count": 1,
    "total_rounds": 8
  }
}
```

**操作数据库：**
- 更新 `ai_chat_sessions`（status='completed', ended_at, session_summary, aha_count）
- 触发异步更新 `ai_student_strategy_profiles`

---

### 7.6 获取会话列表

**接口：** `GET /api/v1/assistant/sessions`
**认证：** 学生 Token

**Query Params：** `?limit=20&before_id=uuid-xxx`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "session_id": "uuid-chat-xxx",
        "session_mode": "free_chat",
        "status": "completed",
        "aha_count": 1,
        "total_rounds": 8,
        "session_summary": "本次对话中...",
        "started_at": "2026-08-30T10:00:00Z",
        "ended_at": "2026-08-30T10:25:00Z"
      }
    ],
    "has_more": false,
    "next_cursor": null
  }
}
```

**操作数据库：** 读 `ai_chat_sessions`（by student_id，时间倒序）

---

## 8. 用户画像模块

### 8.1 获取当前五力画像

**接口：** `GET /api/v1/profile/five-power`
**认证：** 学生 Token
**说明：** 综合能力 = training × 0.6 + test × 0.4

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "has_profile": true,
    "current_ability": {
      "insight": 82,
      "structure": 52,
      "inference": 74,
      "adaptation": 55,
      "transfer": 51
    },
    "latest_test_profile": {
      "profile_id": 2001,
      "forces": {
        "insight": { "ability": 82, "preference": 34, "final": 69, "level": "strong" },
        "structure": { "ability": 47, "preference": 18, "final": 41, "level": "weak" }
      },
      "primary_weakness": "structure",
      "primary_entry": "insight",
      "recommended_mode": "TRAIN_WEAKNESS",
      "preferred_force": "insight",
      "ai_analysis_text": "你的洞察力非常突出...",
      "test_date": "2026-08-30T10:00:00Z"
    },
    "training_profile": {
      "insight_ability": 85,
      "structure_ability": 52,
      "total_training_count": 12,
      "last_trained_at": "2026-08-30T15:00:00Z"
    }
  }
}
```

**操作数据库：** 读 `five_power_profiles`（is_latest=true）、`five_power_training_profiles`

---

### 8.2 获取个人信息

**接口：** `GET /api/v1/profile/me`
**认证：** 学生 Token

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "student_id": 1001,
    "nickname": "小明",
    "avatar_url": "https://...",
    "grade": "G9",
    "subject_prefs": ["MATH", "PHYSICS"],
    "is_minor": true,
    "phone_masked": "138****8000",
    "created_at": "2026-07-01T00:00:00Z"
  }
}
```

**操作数据库：** 读 `students`

---

### 8.3 更新个人信息

**接口：** `PATCH /api/v1/profile/me`
**认证：** 学生 Token

**请求体（可选字段）：**
```json
{
  "nickname": "小明同学",
  "grade": "G10",
  "semester": "S2",
  "subject_prefs": ["MATH"]
}
```

| 字段 | 说明 |
|------|------|
| `grade` | G7~G12，升年级时更新 |
| `semester` | `S1`（上学期）/ `S2`（下学期），开学时更新 |

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "信息更新成功",
  "data": {
    "student_id": 1001,
    "nickname": "小明同学",
    "grade": "G10",
    "semester": "S2"
  }
}
```

**操作数据库：** 更新 `students`

---

### 8.4 查询知识点练习统计（学生自查）

**接口：** `GET /api/v1/profile/kp-stats`
**认证：** 学生 Token

**说明：** 学生查询自己当前年级+学期下各学科的知识点练习统计（练习题数和错误率）。

**Query Params：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `subject_code` | string | 否 | 学科筛选：`MATH` / `PHYSICS` / `CHEMISTRY`，不传返回所有学科 |

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "grade": "G9",
    "semester": "S1",
    "subjects": [
      {
        "subject_code": "MATH",
        "subject_name": "数学",
        "knowledge_points": [
          {
            "kp_id": 1001,
            "kp_name": "二元一次方程组应用题",
            "total_attempts": 12,
            "error_count": 7,
            "error_rate": 58.33,
            "is_stat_valid": true,
            "last_practiced_at": "2026-09-10T15:00:00Z"
          },
          {
            "kp_id": 1008,
            "kp_name": "垂直平分线",
            "total_attempts": 3,
            "error_count": null,
            "error_rate": null,
            "is_stat_valid": false,
            "last_practiced_at": "2026-09-08T10:00:00Z"
          }
        ]
      }
    ]
  }
}
```

**操作数据库：** 读 `student_kp_stats`，关联读 `knowledge_points`、`chapters`、`grades`、`subjects`

---

## 9. 管理后台模块

> 所有管理后台接口路径前缀：`/api/v1/admin/`
> 全部需要 `SUPER_ADMIN` 权限，认证失败返回 `AUTH-003`。
> 所有写操作自动写入 `admin_audit_logs`。

### 9.1 题目管理

> **v1.2.0 重构：** 题库管理接口全面重构，原"新增时手动填写全部字段"方式改为"录入题干 → 触发模型分析 → 审核结果 → 发布/驳回"流程。新增模型分析接口（9.1.2）、发布接口（9.1.5）、驳回接口（9.1.6）。原 AI 标注审核接口（9.2 节）已合并入题目管理流程，不再独立存在。
>
> **v1.3.0 新增：** 新增知识体系树接口（9.1.0），题目列表支持按科目/年级/章节/知识点节点过滤；AI 分析结果新增科目、年级、章节字段；发布接口请求体新增归属体系字段。

#### 9.1.0 知识体系树

**接口：** `GET /api/v1/admin/knowledge-tree`

**说明：** 返回完整的**五级**知识体系树（科目→年级→学期→单元→知识点），用于题库管理左侧导航树渲染。每个节点附带已发布题目数量。

**Query Params：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `with_counts` | boolean | 否 | 是否附带各节点已发布题目数量，默认 true |

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "tree": [
      {
        "type": "subject",
        "id": 1,
        "name": "数学",
        "published_count": 856,
        "children": [
          {
            "type": "grade",
            "id": 3,
            "name": "八年级",
            "code": "G8",
            "published_count": 214,
            "children": [
              {
                "type": "semester",
                "id": 5,
                "code": "S1",
                "name": "上学期",
                "published_count": 120,
                "children": [
                  {
                    "type": "chapter",
                    "id": 12,
                    "name": "第三章：全等三角形",
                    "published_count": 48,
                    "children": [
                      {
                        "type": "knowledge_point",
                        "id": 1005,
                        "name": "全等三角形的判定",
                        "code": "MATH-G8-S1-CH03-KP001",
                        "published_count": 18
                      },
                      {
                        "type": "knowledge_point",
                        "id": 1008,
                        "name": "垂直平分线",
                        "code": "MATH-G8-S1-CH03-KP005",
                        "published_count": 12
                      }
                    ]
                  }
                ]
              },
              {
                "type": "semester",
                "id": 6,
                "code": "S2",
                "name": "下学期",
                "published_count": 94,
                "children": [...]
              }
            ]
          }
        ]
      },
      {
        "type": "subject",
        "id": 2,
        "name": "物理",
        "published_count": 312,
        "children": [...]
      }
    ]
  }
}
```

**节点类型说明：**

| type | 对应表 | 过滤字段 |
|------|--------|---------|
| `subject` | `subjects` | `subject_id` |
| `grade` | `grades` | `grade_id` |
| `semester` | `semesters` | `semester_id` |
| `chapter` | `chapters` | `chapter_id` |
| `knowledge_point` | `knowledge_points` | `knowledge_point_id`（JSONB包含查询）|

**操作数据库：** 读 `subjects`、`grades`、`semesters`、`chapters`、`knowledge_points`，聚合统计 `questions`（status=published）

---

#### 9.1.1 题目列表

**接口：** `GET /api/v1/admin/questions`

**Query Params：**
```
?subject_id=1&grade_id=3&chapter_id=12&knowledge_point_id=1005
&status=pending_review&difficulty=basic&keyword=方程
&limit=20&before_id=5000
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `subject_id` | int | 按科目筛选（点击科目树节点时传入） |
| `grade_id` | int | 按年级筛选（点击年级树节点时传入） |
| `semester_id` | int | 按学期筛选（点击学期树节点时传入，v1.4.0新增）|
| `chapter_id` | int | 按单元筛选（点击单元树节点时传入） |
| `knowledge_point_id` | int | 按知识点筛选（点击知识点树节点时传入） |
| `status` | string | `draft` / `analyzing` / `pending_review` / `published` / `archived` |
| `difficulty` | string | `basic` / `advanced` / `challenge` |
| `primary_power` | string | 五力维度筛选 |
| `keyword` | string | 模糊搜索题干文本 |

> **树节点筛选逻辑（五级）：** 五个层级参数互斥使用（传入更精细的层级时，粗粒度参数忽略）。后端按层级递进查询：`subject_id` → JOIN grades/semesters/chapters/KPs；`grade_id` → JOIN semesters/chapters/KPs；`semester_id` → JOIN chapters/KPs；`chapter_id` → JOIN KPs；`knowledge_point_id` → 直接匹配（JSONB @> 包含查询）。

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "question_id": 5001,
        "stem": "几个人一起买一件东西...",
        "difficulty": "basic",
        "primary_power": "STRUCTURE",
        "status": "published",
        "embedding_status": "completed",
        "analysis_round": 1,
        "subject": { "id": 1, "name": "数学" },
        "grade": { "id": 3, "name": "八年级", "code": "G8" },
        "semester": { "id": 5, "code": "S1", "name": "上学期" },
        "chapter": { "id": 12, "name": "第三章：全等三角形" },
        "knowledge_points": [
          { "id": 1001, "name": "二元一次方程组" },
          { "id": 1003, "name": "应用题建模" }
        ],
        "created_at": "2026-08-01T00:00:00Z"
      }
    ],
    "total": 1280,
    "has_more": true,
    "next_cursor": 5000
  }
}
```

**操作数据库：** 读 `questions`（含 subject_id/grade_id/semester_id/chapter_id），关联读 `subjects`、`grades`、`semesters`、`chapters`、`knowledge_points`

---

#### 9.1.2 录入题干（创建题目）

**接口：** `POST /api/v1/admin/questions`

**权限：** `SUPER_ADMIN`

**说明：** 仅需提交题干和可选配图，其他字段由大模型分析后填充。新增后题目状态为 `draft`。

**请求体：**
```json
{
  "stem": "几个人一起买一件东西。如果每人出400钱，会多出3400钱；如果每人出300钱，会多出100钱。请建立人数与总价之间的方程关系。",
  "image_url": null
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "题目创建成功",
  "data": {
    "question_id": 5100,
    "status": "draft"
  }
}
```

**操作数据库：** 写 `questions`（status=draft），写 `admin_audit_logs`

---

#### 9.1.3 触发模型分析

**接口：** `POST /api/v1/admin/questions/{question_id}/analyze`

**权限：** `SUPER_ADMIN`

**说明：** 对 `draft` 状态的题目触发大模型分析，题目状态变为 `analyzing`。分析为异步执行，前端轮询状态接口（9.1.4）等待完成。

**请求体：** 无（或空 `{}`）

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "模型分析已触发，请稍后刷新查看结果",
  "data": {
    "question_id": 5100,
    "status": "analyzing",
    "analysis_round": 1
  }
}
```

**业务说明：**
- 题目状态由 `draft` 变为 `analyzing`
- 系统向大模型提交 Prompt：包含题干内容、系统角色描述、期望输出格式（含科目/年级/章节/知识点/难度/解析/五力等）
- 大模型返回结构化结果后，系统写入 `question_analyses` 表，题目状态更新为 `pending_review`
- 分析失败（超时/模型错误）时状态回退为 `draft`，记录失败原因

**操作数据库：** 更新 `questions.status='analyzing'`，触发 Celery Queue-B（大模型分析任务）

---

#### 9.1.4 查询题目详情（含分析结果）

**接口：** `GET /api/v1/admin/questions/{question_id}`

**说明：** 返回题目完整信息，包含当前分析结果（用于审核页面渲染）。

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "question_id": 5003,
    "stem": "如图所示，△ABC中，AB=AC，BD是AC的垂线...",
    "image_url": null,
    "status": "pending_review",
    "analysis_round": 1,
    "embedding_status": "pending",
    "created_at": "2026-09-12T10:00:00Z",
    "latest_analysis": {
      "analysis_id": 201,
      "round": 1,
      "rejection_reason_used": null,
      "ai_subject": { "id": 1, "name": "数学" },
      "ai_grade":   { "id": 3, "name": "八年级", "code": "G8" },
      "ai_semester": { "id": 5, "code": "S1", "name": "上学期" },
      "ai_chapter": { "id": 12, "name": "第三章：全等三角形" },
      "ai_knowledge_point_ids": [1005, 1008],
      "ai_knowledge_points": [
        { "id": 1005, "name": "全等三角形的判定" },
        { "id": 1008, "name": "垂直平分线" }
      ],
      "ai_difficulty": "advanced",
      "ai_solution": "证明△ABD≅△CBD（SAS），则AD=CD，故BD是AC的垂直平分线...",
      "ai_common_error": "忽略垂直条件，直接用角度相等证明全等",
      "ai_power_solutions": {
        "INSIGHT":    "识别图形中的隐含对称轴关系",
        "STRUCTURE":  "建立全等三角形的对应关系模型",
        "INFERENCE":  "逐步推导全等条件SAS的满足",
        "ADAPTATION": "检验结论是否与题图几何关系一致",
        "TRANSFER":   "迁移到等腰三角形的轴对称性场景"
      },
      "ai_five_power_weights": {
        "INSIGHT": 3, "STRUCTURE": 2, "INFERENCE": 3, "ADAPTATION": 1, "TRANSFER": 1
      },
      "ai_transfer_directions": ["全等三角形应用", "角平分线性质"],
      "ai_question_type": "APPLICATION",
      "ai_answer": {
        "type": "APPLICATION",
        "final_answer": "BD是AC的垂直平分线（即AD=CD）",
        "key_steps": [
          "在△ABD和△CBD中，AB=CB，BD=BD，∠ADB=∠CDB=90°",
          "由SAS可得△ABD≅△CBD，故AD=CD"
        ]
      },
      "ai_reasoning": "本题要求证明BD是垂直平分线，需要先证明两个三角形全等...",
      "analysis_status": "completed",
      "created_at": "2026-09-12T10:05:00Z"
    },
    "confirmed_data": null
  }
}
```

**`latest_analysis` 字段说明：**

| 字段 | 说明 |
|------|------|
| `ai_subject` | AI 识别的所属科目 |
| `ai_grade` | AI 识别的所属年级 |
| `ai_semester` | AI 识别的所属学期（S1上学期/S2下学期）|
| `ai_chapter` | AI 识别的所属单元（字段名沿用 chapter，语义为单元）|
| `ai_knowledge_points` | AI 识别的知识点列表（多个） |
| `ai_question_type` | AI 识别的题目类型：`SINGLE_CHOICE` / `MULTIPLE_CHOICE` / `FILL_BLANK` / `TRUE_FALSE` / `APPLICATION` |
| `ai_answer` | AI 生成的标准答案（依题型结构化，详见答案格式说明）|
| `ai_five_power_weights` | 各维度权重，整数，总计必须等于10 |
| `rejection_reason_used` | 首次分析（round=1）为 null；驳回重分析（round≥2）为上次驳回意见 |

**ai_answer 各题型格式说明：**

| 题目类型 | `ai_answer` 结构示例 |
|---------|---------------------|
| `SINGLE_CHOICE` | `{"type":"SINGLE_CHOICE","correct":"B"}` |
| `MULTIPLE_CHOICE` | `{"type":"MULTIPLE_CHOICE","correct":["A","C"]}` |
| `FILL_BLANK` | `{"type":"FILL_BLANK","correct":["33","9800"],"accept_range":null}` |
| `TRUE_FALSE` | `{"type":"TRUE_FALSE","correct":true}` |
| `APPLICATION` | `{"type":"APPLICATION","final_answer":"x=33，y=9800","key_steps":["设人数x，总价y","联立方程组求解"]}` |

**操作数据库：** 读 `questions`，关联读 `question_analyses`（最新一条），关联读 `subjects`、`grades`、`semesters`、`chapters`、`knowledge_points`

---

#### 9.1.5 发布题目（审核通过）

**接口：** `POST /api/v1/admin/questions/{question_id}/publish`

**权限：** `SUPER_ADMIN`

**说明：** 管理员在审核页面确认分析结果（可修改字段）后发布题目。请求体携带管理员确认（或修改）后的完整字段，包含归属体系信息。

**请求体：**
```json
{
  "subject_id": 1,
  "grade_id": 3,
  "semester_id": 5,
  "chapter_id": 12,
  "knowledge_point_ids": [1005, 1008],
  "difficulty": "advanced",
  "solution": "证明△ABD≅△CBD（SAS），则AD=CD，故BD是AC的垂直平分线...",
  "common_error": "忽略垂直条件，直接用角度相等证明全等",
  "power_solutions": {
    "INSIGHT":    "识别图形中的隐含对称轴关系",
    "STRUCTURE":  "建立全等三角形的对应关系模型",
    "INFERENCE":  "逐步推导全等条件SAS的满足",
    "ADAPTATION": "检验结论是否与题图几何关系一致",
    "TRANSFER":   "迁移到等腰三角形的轴对称性场景"
  },
  "five_power_weights": {
    "INSIGHT": 3, "STRUCTURE": 2, "INFERENCE": 3, "ADAPTATION": 1, "TRANSFER": 1
  },
  "transfer_directions": ["全等三角形应用", "角平分线性质"],
  "question_type": "APPLICATION",
  "answer": {
    "type": "APPLICATION",
    "final_answer": "BD是AC的垂直平分线（即AD=CD）",
    "key_steps": [
      "在△ABD和△CBD中，AB=CB，BD=BD，∠ADB=∠CDB=90°",
      "由SAS可得△ABD≅△CBD，故AD=CD"
    ]
  }
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "题目已发布，向量化任务已触发",
  "data": {
    "question_id": 5003,
    "status": "published",
    "embedding_status": "pending"
  }
}
```

**业务说明：**
- 题目状态变为 `published`
- `subject_id`、`grade_id`、`semester_id`、`chapter_id`、`knowledge_point_ids` 写入 `questions` 表，驱动五级树节点计数更新
- `primary_power` 由系统自动取 `five_power_weights` 中最大值对应维度写入
- 异步触发 Celery Queue-A：题目向量化入库 ChromaDB

**操作数据库：** 更新 `questions`（写入全部确认字段），触发向量化，写 `admin_audit_logs`

---

#### 9.1.6 驳回题目（审核不通过）

**接口：** `POST /api/v1/admin/questions/{question_id}/reject`

**权限：** `SUPER_ADMIN`

**请求体：**
```json
{
  "rejection_reason": "五力权重中推演力比例偏低，该题主要考查推演力，应将推演力权重提升至5分以上，并补充推演力的具体训练切入点"
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "已驳回，大模型正在重新分析",
  "data": {
    "question_id": 5003,
    "status": "analyzing",
    "analysis_round": 2
  }
}
```

**操作数据库：** 更新 `questions`（status=analyzing，rejection_reason，analysis_round+1），触发 Celery Queue-B，写 `admin_audit_logs`

---

#### 9.1.7 下架题目

**接口：** `PATCH /api/v1/admin/questions/{question_id}/status`

**请求体：** `{ "status": "archived" }`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "题目已下架",
  "data": { "question_id": 5001, "status": "archived" }
}
```

**操作数据库：** 更新 `questions.status='archived'`，ChromaDB 对应向量标记 `is_active=false`，写 `admin_audit_logs`

---

#### 9.1.8 软删除题目

**接口：** `DELETE /api/v1/admin/questions/{question_id}`

**说明：** 仅允许删除 `draft` 状态的题目。

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "题目已删除"
}
```

**操作数据库：** 更新 `questions.deleted_at`，写 `admin_audit_logs`

---

#### 9.1.9 批量导入题干

**接口：** `POST /api/v1/admin/questions/batch-import`

**Content-Type：** `multipart/form-data`

**请求：** `file`：JSON 文件，格式：`[{"stem": "题干1", "image_url": null}, ...]`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "批量导入任务已提交",
  "data": { "import_task_id": "task-batch-20260912", "total_count": 50, "status": "processing" }
}
```

**操作数据库：** 批量写 `questions`（status=analyzing），批量触发 Celery Queue-B

---

### 9.2 AI 标注审核

> **v2.1 已废弃：** 独立的 AI 标注审核模块已移除。审核功能（查看AI分析结果、修改字段、发布/驳回）已完全集成至题库管理模块（9.1节的审核分析结果页）。本节不再提供独立接口。

---

### 9.3 五力测试题维护

> **v1.2.0 新增：** 新增题目创建、状态变更（发布/下架）接口，原 `is_active` 字段已升级为 `status` 三态枚举（`draft` / `published` / `archived`）。
>
> **v1.3.0 变更：** 新增/编辑接口字段重构——去除 `target_power`（目标维度）、`power_weight`（五力贡献权重）、`option_scores`（简单得分）三个字段；答案结构统一改用 `answers` 数组，每条答案包含 `text`（内容）和 `force_weights`（五力权重向量）；选项编号（key）由后端按答案顺序自动生成（A/B/C/D…）；列表筛选去除 `target_power` 参数。
>
> **v1.4.0 变更：** 进一步精简接口字段——去除 `question_type`（题目类型）和 `correct_option`（最优答案）字段（五力测试题所有答案均无唯一正确选项）；列表接口移除 `question_type` 筛选参数；新增 `description` 字段（管理员用题目描述/元数据，不展示给学生）。

#### 9.3.1 获取测试题目列表

**接口：** `GET /api/v1/admin/cognitive-questions`

**查询参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `status` | string | 否 | 按状态筛选：`draft` / `published` / `archived`，不传返回全部 |
| `keyword` | string | 否 | 模糊搜索题干或题目描述 |

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "published_count": 18,
    "test_available": false,
    "list": [
      {
        "id": 1,
        "question_no": 1,
        "display_order": 1,
        "description": "测量学生观察冲突证据时的洞察力倾向",
        "stem": "有一个封闭房间...",
        "image_url": null,
        "answers": [
          {
            "key": "A",
            "text": "三个杯子的水最后都会变凉",
            "force_weights": { "INSIGHT": 1, "CONSTRUCT": 1, "DEDUCE": 0, "ADAPT": 7, "MIGRATE": 1 }
          },
          {
            "key": "B",
            "text": "中间杯子的水最后变凉最慢",
            "force_weights": { "INSIGHT": 7, "CONSTRUCT": 1, "DEDUCE": 1, "ADAPT": 0, "MIGRATE": 1 }
          },
          {
            "key": "C",
            "text": "靠近热源的杯子最后变凉最慢",
            "force_weights": { "INSIGHT": 3, "CONSTRUCT": 2, "DEDUCE": 2, "ADAPT": 2, "MIGRATE": 1 }
          },
          {
            "key": "D",
            "text": "三杯水都不会变凉",
            "force_weights": { "INSIGHT": 1, "CONSTRUCT": 1, "DEDUCE": 1, "ADAPT": 1, "MIGRATE": 6 }
          }
        ],
        "reference_time_sec": 90,
        "status": "published",
        "created_by": 1,
        "updated_by": 2,
        "created_at": "2026-08-01T08:00:00Z",
        "updated_at": "2026-09-12T14:30:00Z"
      }
    ]
  }
}
```

**字段说明：**

| 字段 | 说明 |
|------|------|
| `published_count` | 当前已发布题目数量 |
| `test_available` | 是否满足测试条件（已发布数 = 20） |
| `description` | 题目描述（管理员元数据，不展示给学生，可空） |
| `answers[].key` | 选项编号，由后端按顺序自动生成（A、B、C、D…） |
| `answers[].force_weights` | 该答案的五力权重向量，五项均为非负整数，之和 = 10 |

**操作数据库：** 读 `cognitive_test_questions`

---

#### 9.3.2 新增测试题目

**接口：** `POST /api/v1/admin/cognitive-questions`

**权限：** `SUPER_ADMIN`

**请求体：**
```json
{
  "description": "测量学生观察冲突证据时的洞察力倾向",
  "stem": "有一个封闭房间，里面放着三杯热水...",
  "image_url": null,
  "answers": [
    {
      "text": "三个杯子的水最后都会变凉",
      "force_weights": { "INSIGHT": 1, "CONSTRUCT": 1, "DEDUCE": 0, "ADAPT": 7, "MIGRATE": 1 }
    },
    {
      "text": "B和C都加了盖，但结果仍然不同",
      "force_weights": { "INSIGHT": 7, "CONSTRUCT": 1, "DEDUCE": 1, "ADAPT": 0, "MIGRATE": 1 }
    },
    {
      "text": "靠近热源的杯子最后变凉最慢",
      "force_weights": { "INSIGHT": 3, "CONSTRUCT": 2, "DEDUCE": 2, "ADAPT": 2, "MIGRATE": 1 }
    },
    {
      "text": "三杯水都不会变凉",
      "force_weights": { "INSIGHT": 1, "CONSTRUCT": 1, "DEDUCE": 1, "ADAPT": 1, "MIGRATE": 6 }
    }
  ],
  "reference_time_sec": 90
}
```

**响应（成功）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "题目创建成功",
  "data": {
    "id": 25,
    "status": "draft",
    "created_at": "2026-09-12T10:00:00Z"
  }
}
```

**业务规则：**
- 新建题目默认状态为 `draft`
- `answers` 数组长度必须在 2~8 之间
- 请求体中 `answers` 不含 `key` 字段，后端按数组顺序自动生成（A、B、C、D…）
- 每条答案的 `force_weights` 五项均为非负整数，之和必须等于 `10`
- `description` 为选填字段，用于管理员记录题目测量意图，不影响评分逻辑
- `display_order` 由系统自动分配（当前最大值 + 1）

**操作数据库：** 写入 `cognitive_test_questions`，写入 `admin_audit_logs`

---

#### 9.3.3 编辑测试题目

**接口：** `PATCH /api/v1/admin/cognitive-questions/{id}`

**权限：** `SUPER_ADMIN`

**说明：** 支持对 `draft`、`published`、`archived` **三种状态**的题目进行编辑。所有字段均为可选，仅传入需修改的字段。已下架题目可在此状态下修改内容，完成后通过状态变更接口重新发布。

**请求体：**
```json
{
  "description": "修改后的题目描述（元数据）",
  "stem": "修改后的题干内容...",
  "image_url": "https://oss.example.com/img/q25.png",
  "answers": [
    {
      "text": "更新后的答案A",
      "force_weights": { "INSIGHT": 7, "CONSTRUCT": 1, "DEDUCE": 1, "ADAPT": 0, "MIGRATE": 1 }
    },
    {
      "text": "更新后的答案B",
      "force_weights": { "INSIGHT": 1, "CONSTRUCT": 1, "DEDUCE": 0, "ADAPT": 7, "MIGRATE": 1 }
    },
    {
      "text": "更新后的答案C",
      "force_weights": { "INSIGHT": 3, "CONSTRUCT": 2, "DEDUCE": 2, "ADAPT": 2, "MIGRATE": 1 }
    }
  ],
  "reference_time_sec": 90
}
```

**说明：**
- `answers` 传入时为**全量替换**（不支持部分更新），后端重新按顺序生成 key（A/B/C…）

**响应（成功）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "题目已更新",
  "data": { "id": 1, "updated_at": "2026-09-12T11:00:00Z" }
}
```

**业务规则：**
- **历史成绩保护：** 每次测试会话创建时系统对当前题目内容生成不可变快照。此后无论对题目做任何修改（含下架编辑后重新发布），**历史测试会话的评分结果永远基于创建时的快照版本，不会变化**
- 对 `published` 题目的修改立即生效，仅影响修改后**新建**的测试会话
- 对 `archived` 题目的修改在重新发布后才对新测试生效，对历史成绩无影响

**操作数据库：** 更新 `cognitive_test_questions`，写入 `admin_audit_logs`（含变更前后字段快照）

---

#### 9.3.4 变更题目状态（发布 / 下架）

**接口：** `PATCH /api/v1/admin/cognitive-questions/{id}/status`

**权限：** `SUPER_ADMIN`

**请求体：**
```json
{
  "status": "published"
}
```

**`status` 合法值：**

| 值 | 说明 | 前置状态要求 |
|----|------|------------|
| `published` | 发布题目（含重新发布下架后编辑的题目） | 来源状态为 `draft` 或 `archived` |
| `archived` | 下架题目 | 来源状态为 `published` |

**响应（成功）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "题目已发布",
  "data": {
    "id": 25,
    "status": "published",
    "published_count": 19,
    "test_available": false
  }
}
```

**业务规则：**
- **发布（含重新发布）：**
  - 当前已发布数 ≥ 20 时拒绝发布，返回 `COGQ-001` 错误
  - `archived` 题目可先在下架状态下完成内容修改，再通过此接口重新发布
  - 重新发布后，历史测试成绩不受任何影响（历史会话依赖快照，非当前题目内容）
- **下架：** 始终允许下架；下架后题目进入可编辑的 `archived` 状态；下架后若已发布数 < 10，返回体附带 `"test_available": false` 警告
- 状态变更写入 `admin_audit_logs`

**错误码：**

| 错误码 | HTTP | 说明 | 端侧提示 |
|--------|------|------|---------|
| `COGQ-001` | 400 | 已发布题目已达20道上限 | 「已发布题目已达上限（20/20），请先下架一道题目后再发布」 |
| `COGQ-002` | 400 | 非法状态转换 | 「当前题目状态不支持此操作」 |
| `COGQ-003` | 404 | 题目不存在 | 「题目不存在或已删除」 |

**操作数据库：** 更新 `cognitive_test_questions.status`，写入 `admin_audit_logs`

---

#### 9.3.5 删除草稿题目

**接口：** `DELETE /api/v1/admin/cognitive-questions/{id}`

**权限：** `SUPER_ADMIN`

**说明：** 仅允许删除 `draft` 状态的题目（物理删除）。`published` 和 `archived` 状态题目不可删除。

**响应（成功）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "草稿题目已删除"
}
```

**错误码：**

| 错误码 | HTTP | 说明 |
|--------|------|------|
| `COGQ-005` | 409 | 非草稿状态题目不可删除 |

**操作数据库：** 物理删除 `cognitive_test_questions` 记录，写入 `admin_audit_logs`

---

### 9.4 训练参数配置

> **v2.1 更新：** 训练参数配置专注于**五维度选题参数**（每维度独立版本化管理），全局算法参数（RAG/EWMA）已移至系统参数管理，五力薄弱加权参数已移除。

#### 9.4.3 获取维度训练参数（当前生效版本）

**接口：** `GET /api/v1/admin/training/dimension-config/active`

**Query Params：** `?dimension=KNOWLEDGE_POINT`

| `dimension` 可选值 | 说明 |
|-------------------|------|
| `KNOWLEDGE_POINT` | 按知识点 |
| `UNIT` | 按单元 |
| `SEMESTER` | 按学期 |
| `ERROR_QUESTIONS` | 错题集 |
| `RANDOM` | 随机练习 |

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "config_id": 12,
    "dimension": "ERROR_QUESTIONS",
    "version_number": "v2",
    "is_active": true,
    "config": {
      "questions_per_session": 5,
      "dedup_window_size": 10,
      "difficulty_ratio": null,
      "wrong_priority": "most_failed",
      "new_question_mix_ratio": 0.20,
      "remove_after_correct": 3
    },
    "created_at": "2026-09-01T00:00:00Z"
  }
}
```

**操作数据库：** 读 `training_dimension_configs`（dimension=X AND is_active=true）

---

#### 9.4.4 创建维度训练参数新版本

**接口：** `POST /api/v1/admin/training/dimension-config`

**请求体（错题集示例）：**
```json
{
  "dimension": "ERROR_QUESTIONS",
  "config": {
    "questions_per_session": 5,
    "dedup_window_size": 10,
    "difficulty_ratio": null,
    "wrong_priority": "most_failed",
    "new_question_mix_ratio": 0.20,
    "remove_after_correct": 3
  },
  "description": "调整错题集移除阈值为3次"
}
```

**请求体（按知识点示例）：**
```json
{
  "dimension": "KNOWLEDGE_POINT",
  "config": {
    "questions_per_session": 5,
    "dedup_window_size": 30,
    "difficulty_ratio": {"basic": 50, "advanced": 35, "challenge": 15}
  },
  "description": "提升进阶题比例"
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "维度训练参数新版本已生效",
  "data": {
    "config_id": 15,
    "dimension": "ERROR_QUESTIONS",
    "version_number": "v3",
    "is_active": true
  }
}
```

**业务说明：**
- 同一维度同时只有一个 `is_active=true`，新版本生效时该维度旧版本自动关闭
- `difficulty_ratio` 传 null 表示按原题难度（适用于错题集）
- 各难度比例之和必须等于100（difficulty_ratio 非 null 时）
- `new_question_mix_ratio` 仅在 dimension=ERROR_QUESTIONS 时有效

**操作数据库：** 写 `training_dimension_configs`，旧版本 is_active→false，写 `admin_audit_logs`

---

#### 9.4.5 获取维度参数历史版本列表

**接口：** `GET /api/v1/admin/training/dimension-config/history`

**Query Params：** `?dimension=KNOWLEDGE_POINT&limit=10`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "dimension": "KNOWLEDGE_POINT",
    "list": [
      {
        "config_id": 12,
        "version_number": "v3",
        "is_active": true,
        "description": "调整难度分布",
        "created_at": "2026-09-01T00:00:00Z"
      },
      {
        "config_id": 8,
        "version_number": "v2",
        "is_active": false,
        "description": "初始配置",
        "created_at": "2026-08-01T00:00:00Z"
      }
    ]
  }
}
```

**操作数据库：** 读 `training_dimension_configs`（按维度，创建时间倒序）

---

#### 9.4.6 获取错题集统计摘要

**接口：** `GET /api/v1/training/wrong-summary`
**认证：** 学生 Token

**Query Params：** `?subject_code=MATH`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "subject_code": "MATH",
    "total_pending": 32,
    "by_chapter": [
      {
        "chapter_id": 12,
        "chapter_name": "第四章：二元一次方程组",
        "pending_count": 12,
        "error_rate": 58.33
      },
      {
        "chapter_id": 9,
        "chapter_name": "第三章：一次函数",
        "pending_count": 8,
        "error_rate": 41.20
      }
    ]
  }
}
```

**操作数据库：** 读 `wrong_answer_records`（review_status IN ('unreview', 'reviewing')），关联 `questions`（subject_code过滤），聚合统计

---

### 9.5 系统参数配置

#### 9.5.1 获取系统配置列表

**接口：** `GET /api/v1/admin/system-configs`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "configs": [
      {
        "key": "active_llm_provider",
        "value": "tongyi",
        "value_type": "string",
        "description": "当前LLM供应商，支持热切换"
      },
      {
        "key": "chat_max_rounds",
        "value": "30",
        "value_type": "number",
        "description": "单会话最大对话轮数"
      }
    ]
  }
}
```

---

#### 9.5.2 更新系统参数（热更新）

**接口：** `PATCH /api/v1/admin/system-configs/{key}`

**请求体：**
```json
{
  "value": "openai"
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "参数已更新，立即生效（无需重启）",
  "data": {
    "key": "active_llm_provider",
    "old_value": "tongyi",
    "new_value": "openai",
    "updated_at": "2026-08-30T10:00:00Z"
  }
}
```

**操作数据库：** 更新 `system_configs`，清除 Redis 缓存（`DEL config:{key}`）

---

### 9.6 启发策略管理

#### 9.6.1 获取策略列表

**接口：** `GET /api/v1/admin/strategies`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "strategy_id": "S-SOCRATIC",
        "strategy_name": "苏格拉底提问",
        "description": "通过连续小问题引导学生自主推导，不给结论",
        "applicable_powers": ["INSIGHT", "STRUCTURE", "INFERENCE", "ADAPTATION", "TRANSFER"],
        "default_priority": 1,
        "example_phrase": "首先，题目告诉我们什么？然后，我们要找的是什么？",
        "is_active": true
      }
    ]
  }
}
```

---

#### 9.6.2 更新策略配置

**接口：** `PATCH /api/v1/admin/strategies/{strategy_id}`

**请求体：**
```json
{
  "default_priority": 2,
  "description": "更新后的策略描述（≤50字）",
  "is_active": true
}
```

---

### 9.7 数据看板

> 数据看板所有接口仅 `SUPER_ADMIN` 可访问，支持统一时间范围参数 `days`（天数，如 7 / 30）过滤。

#### 9.7.1 概览指标卡

**接口：** `GET /api/v1/admin/dashboard/stats`

**Query Params：** `?days=30`（不传默认30天）

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "date": "2026-08-30",
    "users": {
      "total_students": 1024,
      "new_today": 8,
      "active_today": 85,
      "completed_test_total": 823
    },
    "training": {
      "sessions_today": 156,
      "answers_today": 780,
      "accuracy_rate": 0.63,
      "wrong_answers_today": 198
    },
    "ai": {
      "rag_calls_today": 312,
      "chat_sessions_today": 89,
      "chat_messages_today": 890,
      "aha_moments_today": 23,
      "token_cost_today_usd": 0.24,
      "downgrade_rate": 0.02
    },
    "question_bank": {
      "total_published": 1280,
      "total_draft": 124,
      "pending_annotation": 45,
      "pending_embedding": 8
    }
  }
}
```

**操作数据库：** 聚合查询 `students`、`training_sessions`、`training_answer_records`、`ai_chat_sessions`、`ai_strategy_events`、`ai_token_usage_log`、`questions`、`question_annotations`

---

#### 9.7.2 用户增长分析

**接口：** `GET /api/v1/admin/dashboard/user-growth`

**Query Params：** `?days=30`（支持 7 / 30，默认 30）

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "trend": [
      { "date": "2026-08-30", "new_users": 8, "active_users": 85, "dau": 85 }
    ],
    "grade_distribution": [
      { "grade": "G7", "count": 82, "percentage": 0.08 },
      { "grade": "G8", "count": 123, "percentage": 0.12 },
      { "grade": "G9", "count": 225, "percentage": 0.22 },
      { "grade": "G10", "count": 287, "percentage": 0.28 },
      { "grade": "G11", "count": 205, "percentage": 0.20 },
      { "grade": "G12", "count": 102, "percentage": 0.10 }
    ],
    "subject_preference": [
      { "subject": "MATH", "count": 942, "percentage": 0.92 },
      { "subject": "PHYSICS", "count": 768, "percentage": 0.75 },
      { "subject": "CHEMISTRY", "count": 594, "percentage": 0.58 }
    ],
    "retention_funnel": {
      "registered": 1024,
      "completed_test": 823,
      "joined_training": 568,
      "used_assistant": 312,
      "had_aha_moment": 186
    }
  }
}
```

**操作数据库：** 聚合查询 `students`、`test_sessions`、`training_sessions`、`ai_chat_sessions`、`ai_strategy_events`

---

#### 9.7.3 五力测试分析

**接口：** `GET /api/v1/admin/dashboard/five-force-analysis`

**Query Params：** `?days=30`（支持 7 / 30，默认 30）

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "summary": {
      "total_tests": 1024,
      "today_tests": 38,
      "retest_rate": 0.24,
      "completion_rate": 0.85,
      "avg_duration_min": 18
    },
    "avg_scores": {
      "INSIGHT": 68.2,
      "CONSTRUCT": 47.5,
      "DEDUCE": 72.1,
      "ADAPT": 61.3,
      "MIGRATE": 54.8
    },
    "weakness_distribution": [
      { "power": "建构力", "power_key": "CONSTRUCT", "pct": 0.38 },
      { "power": "迁移力", "power_key": "MIGRATE",   "pct": 0.29 },
      { "power": "调适力", "power_key": "ADAPT",     "pct": 0.18 },
      { "power": "推演力", "power_key": "DEDUCE",    "pct": 0.10 },
      { "power": "洞察力", "power_key": "INSIGHT",   "pct": 0.05 }
    ],
    "test_trend": [
      { "date": "2026-08-30", "count": 38, "completion_rate": 0.87 }
    ]
  }
}
```

**操作数据库：** 聚合查询 `test_sessions`、`five_power_profiles`（is_latest=true）

---

#### 9.7.4 训练分析

**接口：** `GET /api/v1/admin/dashboard/training-analysis`

**Query Params：** `?days=30`（支持 7 / 30，默认 30）

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "summary": {
      "total_sessions": 4568,
      "today_sessions": 156,
      "today_answers": 780,
      "overall_accuracy_rate": 0.63,
      "completion_rate": 0.72,
      "early_end_rate": 0.18,
      "abandon_rate": 0.10
    },
    "accuracy_trend": [
      { "date": "2026-08-30", "accuracy_rate": 0.63, "answer_count": 780 }
    ],
    "completion_distribution": {
      "completed": 0.72,
      "early_end": 0.18,
      "abandoned": 0.10
    },
    "force_training_distribution": [
      { "force": "structure", "session_count": 1823, "percentage": 0.40 },
      { "force": "transfer",  "session_count": 1141, "percentage": 0.25 },
      { "force": "adaptation","session_count": 868,  "percentage": 0.19 },
      { "force": "inference", "session_count": 456,  "percentage": 0.10 },
      { "force": "insight",   "session_count": 280,  "percentage": 0.06 }
    ],
    "hot_knowledge_points": [
      { "kp_id": 1001, "name": "二元一次方程组应用题", "session_count": 68 },
      { "kp_id": 1003, "name": "一次函数图像与性质",   "session_count": 55 },
      { "kp_id": 2008, "name": "相似三角形判定",       "session_count": 41 }
    ]
  }
}
```

**操作数据库：** 聚合查询 `training_sessions`、`training_answer_records`、`knowledge_points`

---

#### 9.7.5 AI 助教效果分析

**接口：** `GET /api/v1/admin/dashboard/ai-teaching-analysis`

**Query Params：** `?days=30`（支持 7 / 30，默认 30）

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "summary": {
      "aha_total": 186,
      "avg_rounds": 6.2,
      "breakthrough_rate": 0.24,
      "review_breakthrough_rate": 0.62,
      "silence_rate": 0.08
    },
    "session_mode_distribution": [
      { "mode": "自由提问",     "pct": 0.45, "color": "#3b82f6" },
      { "mode": "错题复盘",     "pct": 0.38, "color": "#f59e0b" },
      { "mode": "训练错题引导", "pct": 0.17, "color": "#22c55e" }
    ],
    "strategy_effectiveness": [
      { "name": "苏格拉底追问", "uses": 3241, "success_rate": 0.81 },
      { "name": "生活类比",     "uses": 2186, "success_rate": 0.74 },
      { "name": "拆解简化",     "uses": 1843, "success_rate": 0.68 },
      { "name": "反例引导",     "uses": 1204, "success_rate": 0.62 },
      { "name": "空间可视化",   "uses": 892,  "success_rate": 0.58 }
    ],
    "aha_trend": [
      { "date": "2026-08-30", "aha_count": 23, "session_count": 89, "message_count": 890 }
    ]
  }
}
```

**操作数据库：** 聚合查询 `ai_chat_sessions`、`ai_chat_messages`、`ai_strategy_events`、`ai_student_strategy_profiles`、`ai_heuristic_strategies`

---

#### 9.7.6 错题集分析

**接口：** `GET /api/v1/admin/dashboard/error-questions-analysis`

**Query Params：** `?days=30`（支持 7 / 30，默认 30）

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "summary": {
      "total_errors": 8432,
      "review_rate": 0.38,
      "breakthrough_rate": 0.62
    },
    "review_status_distribution": [
      { "status": "未复盘",   "count": 5228, "pct": 0.62, "color": "#ef4444" },
      { "status": "已突破",   "count": 1939, "pct": 0.23, "color": "#22c55e" },
      { "status": "待巩固",   "count": 759,  "pct": 0.09, "color": "#f59e0b" },
      { "status": "复盘中",   "count": 506,  "pct": 0.06, "color": "#3b82f6" }
    ],
    "power_distribution": [
      { "power": "建构力", "power_key": "CONSTRUCT", "pct": 0.35 },
      { "power": "推演力", "power_key": "DEDUCE",    "pct": 0.28 },
      { "power": "迁移力", "power_key": "MIGRATE",   "pct": 0.18 },
      { "power": "调适力", "power_key": "ADAPT",     "pct": 0.12 },
      { "power": "洞察力", "power_key": "INSIGHT",   "pct": 0.07 }
    ],
    "top_error_questions": [
      { "rank": 1, "stem": "几个人一起买东西，每人400多出3400...", "error_count": 128, "power": "建构力", "power_key": "CONSTRUCT" },
      { "rank": 2, "stem": "身高每天变成前一天的90%，判断哪个正确...", "error_count": 94, "power": "推演力", "power_key": "DEDUCE" }
    ]
  }
}
```

**操作数据库：** 聚合查询 `wrong_answer_records`、`questions`

---

#### 9.7.7 题库健康度看板

**接口：** `GET /api/v1/admin/dashboard/question-bank-health`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "status_distribution": [
      { "status": "published", "count": 1280, "percentage": 0.77 },
      { "status": "draft",     "count": 124,  "percentage": 0.07 },
      { "status": "archived",  "count": 261,  "percentage": 0.16 }
    ],
    "difficulty_distribution": [
      { "difficulty": "basic",     "count": 640, "percentage": 0.50 },
      { "difficulty": "advanced",  "count": 448, "percentage": 0.35 },
      { "difficulty": "challenge", "count": 192, "percentage": 0.15 }
    ],
    "force_distribution": [
      { "force": "structure", "count": 356, "percentage": 0.28 },
      { "force": "inference", "count": 307, "percentage": 0.24 },
      { "force": "insight",   "count": 256, "percentage": 0.20 },
      { "force": "adaptation","count": 230, "percentage": 0.18 },
      { "force": "transfer",  "count": 131, "percentage": 0.10 }
    ],
    "annotation_progress": {
      "confirmed": 1197,
      "pending":    45,
      "failed":     38,
      "confidence_distribution": {
        "high":   { "range": ">=0.85", "count": 980 },
        "medium": { "range": "0.60~0.85", "count": 217 },
        "low":    { "range": "<0.60",   "count": 0 }
      }
    },
    "embedding_progress": {
      "completed": 1272,
      "pending":      8,
      "failed":       0
    },
    "kp_coverage_alerts": [
      {
        "kp_id": 2015,
        "kp_name": "二次函数最值应用",
        "available_count": 3,
        "status": "insufficient"
      }
    ]
  }
}
```

**操作数据库：** 聚合查询 `questions`、`question_annotations`、`knowledge_points`

---

#### 9.7.8 Token 消耗统计

**接口：** `GET /api/v1/admin/dashboard/token-trend`

**Query Params：** `?days=30`（支持 7 / 30，默认 30）

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "summary": {
      "total_tokens": 2840000,
      "total_cost_usd": 0.427,
      "by_module": {
        "training":  { "tokens": 1800000, "cost_usd": 0.270, "percentage": 0.63 },
        "assistant": { "tokens": 950000,  "cost_usd": 0.143, "percentage": 0.34 },
        "test":      { "tokens": 90000,   "cost_usd": 0.014, "percentage": 0.03 }
      },
      "cost_alert": {
        "threshold_usd": 5.0,
        "today_cost_usd": 0.24,
        "is_exceeded": false
      }
    },
    "daily_data": [
      {
        "date": "2026-08-30",
        "total_tokens": 95000,
        "total_cost_usd": 0.014,
        "by_module": {
          "training": 60000,
          "assistant": 32000,
          "test": 3000
        }
      }
    ],
    "downgrade_stats": {
      "total_calls": 1604,
      "degraded_calls": 32,
      "downgrade_rate": 0.02
    }
  }
}
```

**操作数据库：** 聚合查询 `ai_token_usage_log`、`system_downgrade_log`

---

#### 9.7.9 用户增长详情

> **v2.1 新增**：数据看板首页用户规模卡点击后的详情分析页所需接口，提供更细粒度的增长与留存数据。

**接口：** `GET /api/v1/admin/dashboard/user-growth-detail`

**Query Params：** `?days=30`（支持 7 / 30 / 90，默认 30）

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "daily_new_users": [
      { "date": "2026-08-30", "new_users": 8, "active_users": 85 }
    ],
    "retention_funnel": [
      { "stage": "注册用户",   "count": 1024, "pct": 1.00 },
      { "stage": "完成五力测试", "count": 823, "pct": 0.80 },
      { "stage": "参与训练",   "count": 568, "pct": 0.55 },
      { "stage": "使用AI助教", "count": 312, "pct": 0.30 },
      { "stage": "产生理解突破", "count": 186, "pct": 0.18 }
    ],
    "grade_distribution": [
      { "grade": "初一(G7)", "count": 82,  "pct": 0.08 },
      { "grade": "初二(G8)", "count": 123, "pct": 0.12 },
      { "grade": "初三(G9)", "count": 225, "pct": 0.22 },
      { "grade": "高一(G10)","count": 287, "pct": 0.28 },
      { "grade": "高二(G11)","count": 205, "pct": 0.20 },
      { "grade": "高三(G12)","count": 102, "pct": 0.10 }
    ],
    "subject_preference": [
      { "subject": "数学", "pct": 0.92 },
      { "subject": "物理", "pct": 0.75 },
      { "subject": "化学", "pct": 0.58 }
    ]
  }
}
```

**操作数据库：** 聚合查询 `students`、`test_sessions`、`training_sessions`、`ai_chat_sessions`、`ai_strategy_events`

---

### 9.8 用户管理

> **v1.2.0 变更：** 原"学生管理"（9.8节）扩展为"用户管理"，新增家长列表、管理员列表、管理员创建/停用/重置密码、绑定解绑管理等接口。`bind_method` 新增 `PARENT_SELF` 值。

---

#### 9.8.1 学生列表

**接口：** `GET /api/v1/admin/students`

**Query Params：**

| 参数 | 类型 | 说明 |
|------|------|------|
| `grade` | string | 年级筛选：G7~G12 |
| `has_profile` | boolean | 是否已有五力画像 |
| `keyword` | string | 模糊搜索昵称或手机号（脱敏后）|
| `limit` | int | 每页数量，默认20 |
| `before_id` | int | 游标分页起始ID |

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "user_id": 1001,
        "user_type": "STUDENT",
        "nickname": "小明",
        "grade": "G9",
        "is_minor": true,
        "has_five_power_profile": true,
        "primary_weakness": "STRUCTURE",
        "training_count": 15,
        "bound_parents_count": 2,
        "last_login_at": "2026-08-30T09:00:00Z",
        "created_at": "2026-07-01T00:00:00Z"
      }
    ],
    "total": 483,
    "has_more": true,
    "next_cursor": 1000
  }
}
```

**操作数据库：** 查询 `students`（`user_type='STUDENT'`）+ 聚合 `five_power_profiles`、`training_sessions`、`parent_student_bindings`

---

#### 9.8.2 家长列表

**接口：** `GET /api/v1/admin/parents`

**Query Params：** `?keyword=小明爸爸&limit=20&before_id=2000`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "user_id": 2001,
        "user_type": "PARENT",
        "nickname": "小明爸爸",
        "phone_masked": "138****8888",
        "is_confirmed": true,
        "bound_students": [
          { "student_id": 1001, "nickname": "小明", "grade": "G9", "bind_method": "ADMIN", "bind_date": "2026-07-05T00:00:00Z" }
        ],
        "last_login_at": "2026-08-29T10:00:00Z",
        "created_at": "2026-07-05T00:00:00Z"
      }
    ],
    "total": 156,
    "has_more": true,
    "next_cursor": 2000
  }
}
```

**字段说明：**
- `is_confirmed`：管理员代创建的家长账号在家长首次登录前为 `false`（待激活状态）
- `phone_masked`：手机号脱敏展示
- `bind_method`：`INVITE_CODE`（邀请码）/ `PARENT_SELF`（家长主动）/ `ADMIN`（管理员）

**操作数据库：** 查询 `students`（`user_type='PARENT'`）+ 关联 `parent_student_bindings`

---

#### 9.8.3 管理员账号列表

**接口：** `GET /api/v1/admin/admin-users`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "admin_id": 1,
        "username": "admin001",
        "display_name": "系统管理员",
        "phone_masked": "138****1111",
        "is_active": 1,
        "last_login_at": "2026-09-12T08:00:00Z",
        "created_at": "2026-08-01T00:00:00Z"
      }
    ]
  }
}
```

**操作数据库：** 查询 `admin_users`

---

#### 9.8.4 创建管理员账号

**接口：** `POST /api/v1/admin/admin-users`

**请求体：**
```json
{
  "username": "ops001",
  "display_name": "运营专员",
  "phone": "13900000000",
  "email": "ops001@mesh-edu.com"
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "管理员账号已创建，初始密码已发送至手机",
  "data": {
    "admin_id": 5,
    "username": "ops001"
  }
}
```

**业务规则：**
- 系统自动生成随机初始密码发送至手机号
- 新账号 `is_active = 2`（待首次登录），首次登录必须修改密码
- 写入 `admin_audit_logs`

**操作数据库：** 写 `admin_users`，写 `admin_audit_logs`

---

#### 9.8.5 启用 / 停用管理员账号

**接口：** `PUT /api/v1/admin/admin-users/{admin_id}/status`

**请求体：**
```json
{ "is_active": 0 }
```

| 值 | 含义 |
|----|------|
| `0` | 停用（禁止登录，当前Token立即失效）|
| `1` | 启用 |

**业务规则：** 不得停用自己的账号；操作写入 `admin_audit_logs`

**操作数据库：** 更新 `admin_users.is_active`，写 `admin_audit_logs`

---

#### 9.8.6 重置管理员密码

**接口：** `POST /api/v1/admin/admin-users/{admin_id}/reset-password`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "临时密码已发送至管理员手机"
}
```

**操作数据库：** 更新 `admin_users.password_hash`，写 `admin_audit_logs`

---

#### 9.8.7 学生画像（用户详情）

> 聚合学生的认知能力、学习行为、AI 互动效果、错题分布、成长轨迹六大维度，供管理员全面了解学生学习状况。

**接口：** `GET /api/v1/admin/students/{student_id}`

**Query Params：** `?date_range=30d`（可选，影响行为/AI互动统计范围，默认全部）

**认证：** SUPER_ADMIN Token

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "basic_info": {
      "student_id": 1001,
      "nickname": "小明",
      "grade": "G9",
      "subject_prefs": ["MATH", "PHYSICS"],
      "is_minor": true,
      "registered_at": "2026-07-01T00:00:00Z",
      "last_active_at": "2026-08-30T09:00:00Z",
      "active_days_total": 61,
      "bound_parents_count": 2,
      "bound_parents": [
        { "parent_id": 2001, "nickname": "小明爸爸", "bind_date": "2026-07-05T00:00:00Z" },
        { "parent_id": 2002, "nickname": "小明妈妈", "bind_date": "2026-08-01T00:00:00Z" }
      ]
    },

    "five_power_profile": {
      "has_profile": true,
      "test_count": 3,
      "latest_test_at": "2026-08-30T10:00:00Z",
      "profile_confidence": 0.82,
      "current": {
        "profile_id": 2001,
        "forces": {
          "insight":    { "ability": 82, "preference": 34, "final": 69, "level": "strong" },
          "structure":  { "ability": 47, "preference": 18, "final": 41, "level": "weak"   },
          "inference":  { "ability": 74, "preference": 17, "final": 57, "level": "good"   },
          "adaptation": { "ability": 61, "preference": 21, "final": 49, "level": "medium" },
          "transfer":   { "ability": 51, "preference": 10, "final": 39, "level": "weak"   }
        },
        "primary_weakness": "structure",
        "secondary_weakness": "transfer",
        "primary_strength": "insight",
        "preferred_force": "insight",
        "recommended_mode": "TRAIN_WEAKNESS",
        "ai_analysis_text": "该生洞察力突出，擅长从题目条件中抓取关键信息；建构力明显偏弱，面对复杂应用题时难以建立变量关系，建议从洞察入口切入建构力训练..."
      },
      "training_profile": {
        "insight_ability": 85,
        "structure_ability": 48,
        "inference_ability": 76,
        "adaptation_ability": 63,
        "transfer_ability": 54,
        "total_training_count": 15,
        "last_trained_at": "2026-08-30T15:00:00Z"
      }
    },

    "learning_behavior": {
      "total_sessions": 15,
      "total_questions": 75,
      "overall_accuracy": 0.65,
      "active_days_recent_7": 5,
      "avg_session_duration_min": 12.3,
      "session_completion": {
        "completed_rate": 0.72,
        "early_end_rate": 0.18,
        "abandoned_rate": 0.10
      },
      "force_accuracy": {
        "insight":    0.92,
        "structure":  0.41,
        "inference":  0.78,
        "adaptation": 0.63,
        "transfer":   0.55
      },
      "hot_knowledge_points": [
        { "kp_id": 1001, "name": "二元一次方程组应用题", "session_count": 8 },
        { "kp_id": 1003, "name": "一次函数图像与性质",   "session_count": 4 },
        { "kp_id": 2008, "name": "相似三角形判定",       "session_count": 3 }
      ],
      "accuracy_trend_weekly": [
        { "week": "2026-08-04", "accuracy": 0.55 },
        { "week": "2026-08-11", "accuracy": 0.58 },
        { "week": "2026-08-18", "accuracy": 0.61 },
        { "week": "2026-08-25", "accuracy": 0.65 }
      ]
    },

    "ai_interaction": {
      "total_sessions": 23,
      "total_messages": 143,
      "avg_rounds_per_session": 6.2,
      "aha_count": 18,
      "aha_rate": 0.78,
      "silence_triggered_count": 2,
      "session_mode_distribution": {
        "wrong_review":            0.45,
        "training_error_guidance": 0.35,
        "free_chat":               0.20
      },
      "top_effective_strategies": [
        { "strategy_id": "S-SOCRATIC",     "name": "苏格拉底提问", "effectiveness_score": 85.2 },
        { "strategy_id": "S-ANALOGY-LIFE", "name": "生活场景类比", "effectiveness_score": 72.4 },
        { "strategy_id": "S-COUNTER",      "name": "反例证伪",     "effectiveness_score": 61.0 }
      ],
      "last_chat_at": "2026-08-30T11:30:00Z"
    },

    "wrong_answer_profile": {
      "total_wrong": 42,
      "unreview_count": 18,
      "review_rate": 0.57,
      "aha_rate_in_reviewed": 0.62,
      "recent_7d_new_wrong": 8,
      "force_distribution": [
        { "force": "structure",  "count": 22, "percentage": 0.52 },
        { "force": "transfer",   "count": 10, "percentage": 0.24 },
        { "force": "adaptation", "count": 6,  "percentage": 0.14 },
        { "force": "inference",  "count": 3,  "percentage": 0.07 },
        { "force": "insight",    "count": 1,  "percentage": 0.02 }
      ],
      "top_wrong_knowledge_points": [
        { "kp_id": 1001, "name": "二元一次方程组应用题", "wrong_count": 12, "force": "structure" },
        { "kp_id": 1003, "name": "一次函数实际应用",     "wrong_count": 8,  "force": "structure" },
        { "kp_id": 2008, "name": "相似三角形综合",       "wrong_count": 6,  "force": "inference" }
      ]
    },

    "growth_trajectory": {
      "test_history": [
        {
          "profile_id": 1998,
          "test_date": "2026-07-01T00:00:00Z",
          "forces": {
            "insight": 70, "structure": 50, "inference": 66,
            "adaptation": 56, "transfer": 49
          }
        },
        {
          "profile_id": 2001,
          "test_date": "2026-08-30T10:00:00Z",
          "forces": {
            "insight": 82, "structure": 47, "inference": 74,
            "adaptation": 61, "transfer": 51
          }
        }
      ],
      "growth_vs_last_test": {
        "insight":    { "delta": 12, "trend": "up"   },
        "structure":  { "delta": -3, "trend": "down" },
        "inference":  { "delta": 8,  "trend": "up"   },
        "adaptation": { "delta": 5,  "trend": "up"   },
        "transfer":   { "delta": 2,  "trend": "up"   }
      }
    }
  }
}
```

**操作数据库：**
- `students`（`user_type='STUDENT'`）、`parent_student_bindings`（基础信息）
- `five_power_profiles`（is_latest=true + 历史全量）、`five_power_training_profiles`（五力画像）
- `training_sessions`、`training_answer_records`（学习行为）
- `ai_chat_sessions`、`ai_chat_messages`、`ai_strategy_events`、`ai_student_strategy_profiles`（AI 互动）
- `wrong_answer_records`（错题画像）

**业务约束：**
- 仅 SUPER_ADMIN 可访问
- 行为/AI互动数据支持 `date_range` 参数过滤（7d/30d/all，默认 all）
- 如学生未完成五力测试，`five_power_profile.has_profile = false`，相关字段返回 null

---

#### 9.8.8 管理员绑定家长到学生

**接口：** `POST /api/v1/admin/students/{student_id}/bind-parent`

**请求体：**
```json
{
  "parent_phone": "13900000000"
}
```

**响应（成功，家长账号已存在）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "绑定成功",
  "data": {
    "binding_id": 101,
    "parent_id": 2001,
    "parent_created": false
  }
}
```

**响应（成功，家长账号不存在，自动创建）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "已创建家长账号并完成绑定，激活短信已发送",
  "data": {
    "binding_id": 102,
    "parent_id": 2099,
    "parent_created": true
  }
}
```

**业务规则：**
- 系统按手机号查找家长账号（`user_type='PARENT'`）：存在则直接绑定，不存在则自动创建（`is_confirmed=false`）并发送激活通知短信
- 绑定方式：`bind_method='ADMIN'`，`bind_status='active'`（立即生效）
- 每位学生最多绑定 2 位家长，超出返回 `BIND-002`

**错误码：**

| 错误码 | HTTP | 说明 |
|--------|------|------|
| `BIND-002` | 409 | 该学生已绑定2位家长，无法继续绑定 |
| `BIND-003` | 409 | 该家长已是该学生的绑定家长 |
| `BIND-004` | 409 | 该家长已绑定5位学生，无法继续绑定 |

**操作数据库：** 写/读 `students`（按 phone_hash 查家长），写 `parent_student_bindings`（bind_method='ADMIN'），写 `admin_audit_logs`

---

#### 9.8.9 解除家长-学生绑定

> 提供两个解绑入口：从学生侧或从家长侧均可发起解绑操作。

**接口A（从学生维度解绑）：** `POST /api/v1/admin/students/{student_id}/unbind-parent`

**请求体：**
```json
{ "parent_id": 2001 }
```

**接口B（从家长维度解绑）：** `POST /api/v1/admin/parents/{parent_id}/unbind`

**请求体：**
```json
{ "student_id": 1001 }
```

**响应（两接口相同）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "解绑成功"
}
```

**业务规则：** 软删除（`deleted_at` 置为当前时间，`is_active=FALSE`），保留历史记录；写 `admin_audit_logs`

**操作数据库：** 更新 `parent_student_bindings`，写 `admin_audit_logs`

---


#### 9.8.10 查询学生知识点练习统计（管理员视角）

**接口：** `GET /api/v1/admin/students/{student_id}/kp-stats`

**权限：** `SUPER_ADMIN`

**说明：** 管理员查看指定学生当前年级+学期下各学科的知识点练习统计（练习题数和错误率）。

**Query Params：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `subject_code` | string | 否 | 学科筛选，不传返回所有学科 |
| `only_valid` | boolean | 否 | 为 `true` 时仅返回已统计（≥5题）的知识点，默认 false |

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "student_id": 1001,
    "nickname": "小明",
    "grade": "G9",
    "semester": "S1",
    "stat_summary": {
      "total_kp_count": 12,
      "valid_kp_count": 8,
      "pending_kp_count": 4
    },
    "subjects": [
      {
        "subject_code": "MATH",
        "subject_name": "数学",
        "knowledge_points": [
          {
            "kp_id": 1001,
            "kp_name": "二元一次方程组应用题",
            "chapter_name": "第四章：方程与方程组",
            "total_attempts": 12,
            "error_count": 7,
            "error_rate": 58.33,
            "is_stat_valid": true,
            "last_practiced_at": "2026-09-10T15:00:00Z"
          },
          {
            "kp_id": 1008,
            "kp_name": "垂直平分线",
            "chapter_name": "第三章：全等三角形",
            "total_attempts": 3,
            "error_count": null,
            "error_rate": null,
            "is_stat_valid": false,
            "last_practiced_at": "2026-09-08T10:00:00Z"
          }
        ]
      }
    ]
  }
}
```

**业务说明：**
- 统计范围：`student_kp_stats`，按 `student_id` + 该学生的当前 `grade`/`semester` 关联 `semesters` 表确定 `semester_id`，再 JOIN `chapters`/`knowledge_points` 过滤
- `error_rate` 单位：百分比（58.33 表示 58.33%）
- `is_stat_valid = false` 时 `error_count` 和 `error_rate` 均为 `null`

**操作数据库：** 读 `student_kp_stats`，关联读 `students`、`knowledge_points`、`chapters`、`grades`、`subjects`

---

#### 9.8.11 获取学生AI助教提示词摘要

**接口：** `GET /api/v1/admin/students/{student_id}/ai-prompt-summary`

**权限：** `SUPER_ADMIN`

**说明：** 返回该学生的完整提示词摘要，包含学习信息摘要、个人洞察摘要、洞察条目历史及合并后的完整 Prompt 预览。

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "student_id": 1001,
    "nickname": "小明",
    "is_enabled": true,
    "learning_summary": {
      "content": "学生小明，初三（G9）上学期，主要学习数学和物理。五力测试显示洞察力82分（优势），建构力47分（主要薄弱点），推演力74分，调适力61分，迁移力51分，推荐训练模式为TRAIN_WEAKNESS。近期训练情况：累计训练15次，近7天活跃5天，整体正确率65%。建构力维度正确率仅41%，是当前核心突破方向。高频错题集中在二元一次方程组应用（错误率58%）、一次函数实际应用（41%）。",
      "version": 8,
      "updated_at": "2026-09-12T15:30:00Z"
    },
    "personal_insight": {
      "content": "小明喜欢打篮球，自称是湖人队球迷。近期期中考试压力较大，在与AI对话中提到"最近有点焦虑，感觉数学学不进去"。对游戏角色类比反应积极，aha时刻多出现在此类场景。建议AI助教多用运动/游戏类比，语气轻松不说教。",
      "updated_at": "2026-09-10T11:20:00Z",
      "updated_by": "AI"
    },
    "prompt_preview": "你是一位专属于小明同学的AI数学学习助教。

【学生学习档案】
学生小明，初三（G9）上学期...

【学生个人档案】
小明喜欢打篮球...

请根据以上信息，以贴合该学生特点的方式提供学习辅导。",
    "insights": [
      {
        "id": 45,
        "content": "提到近期期中考试压力，有焦虑情绪",
        "insight_type": "mood",
        "source": "AI",
        "is_sensitive": false,
        "weight": 1.00,
        "created_at": "2026-09-10T11:20:00Z"
      },
      {
        "id": 42,
        "content": "喜欢用游戏类比理解知识，反应积极，aha时刻多",
        "insight_type": "analogy_pref",
        "source": "AI",
        "is_sensitive": false,
        "weight": 1.00,
        "created_at": "2026-09-08T09:45:00Z"
      },
      {
        "id": 38,
        "content": "爸爸比较严格，建议AI语气温和",
        "insight_type": "family",
        "source": "ADMIN",
        "is_sensitive": false,
        "weight": 1.00,
        "created_at": "2026-09-05T14:00:00Z"
      },
      {
        "id": 31,
        "content": "自称湖人球迷，喜欢打篮球",
        "insight_type": "interest",
        "source": "AI",
        "is_sensitive": false,
        "weight": 1.00,
        "created_at": "2026-08-30T16:30:00Z"
      }
    ]
  }
}
```

**操作数据库：** 读 `student_ai_prompt_summaries`，关联读 `student_prompt_insights`（is_active=true，按 created_at DESC）

---

#### 9.8.12 更新个人洞察摘要（管理员手动编辑）

**接口：** `PUT /api/v1/admin/students/{student_id}/ai-prompt-summary`

**权限：** `SUPER_ADMIN`

**请求体：**
```json
{
  "personal_insight": "小明喜欢打篮球，自称是湖人队球迷。近期期中考试压力较大...（管理员修改后的完整洞察文本）"
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "个人洞察摘要已更新",
  "data": {
    "updated_at": "2026-09-12T16:00:00Z",
    "updated_by": "ADMIN"
  }
}
```

**操作数据库：** 更新 `student_ai_prompt_summaries`（personal_insight, insight_updated_by='ADMIN'），写 `admin_audit_logs`

---

#### 9.8.13 新增洞察条目（管理员手动补充）

**接口：** `POST /api/v1/admin/students/{student_id}/prompt-insights`

**权限：** `SUPER_ADMIN`

**请求体：**
```json
{
  "content": "爸爸比较严格，建议AI语气温和",
  "insight_type": "family",
  "is_sensitive": false
}
```

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "洞察条目已添加",
  "data": {
    "insight_id": 38,
    "created_at": "2026-09-12T16:05:00Z"
  }
}
```

**`insight_type` 枚举：**

| 值 | 说明 |
|----|------|
| `interest` | 兴趣爱好 |
| `mood` | 心情/情绪状态 |
| `life_event` | 近期生活事件 |
| `analogy_pref` | 有效类比偏好 |
| `family` | 家庭背景线索 |
| `learning_attitude` | 学习心态 |
| `other` | 其他 |

**操作数据库：** 写 `student_prompt_insights`（source='ADMIN'），异步触发个人洞察摘要重新聚合，写 `admin_audit_logs`

---

#### 9.8.14 重新生成学习信息摘要

**接口：** `POST /api/v1/admin/students/{student_id}/ai-prompt-summary/regenerate`

**权限：** `SUPER_ADMIN`

**说明：** 基于学生当前的五力画像、训练记录、错题统计等结构化数据，异步重新生成学习信息摘要文本。

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "学习信息摘要正在重新生成，约需10秒",
  "data": {
    "task_id": "regen-task-20260912-001"
  }
}
```

**操作数据库：** 触发 Celery Queue-S（摘要生成任务），完成后更新 `student_ai_prompt_summaries.learning_summary` 及版本号

---

### 9.9 审计日志

#### 9.9.1 查询审计日志

**接口：** `GET /api/v1/admin/audit-logs`

**Query Params：** `?admin_id=1&action=CONFIRM_ANNOTATION&limit=20&before_id=5000`

**响应：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "log_id": 5001,
        "admin_id": 1,
        "admin_name": "系统管理员",
        "action": "CONFIRM_ANNOTATION",
        "target_type": "question",
        "target_id": "5050",
        "detail": {
          "before": { "annotation_status": "pending" },
          "after": { "annotation_status": "confirmed" }
        },
        "ip_address": "192.168.1.1",
        "created_at": "2026-08-30T09:00:00Z"
      }
    ],
    "total": 5000,
    "has_more": true,
    "next_cursor": 5000
  }
}
```

---

### 9.10 健康检查

**接口：** `GET /health`
**认证：** 无
**说明：** 供 Nginx 负载均衡器探活使用，不计入访问日志

**响应（正常）：**
```json
{
  "status": "success",
  "code": 0,
  "msg": "服务正常",
  "data": {
    "status": "healthy",
    "checks": {
      "database": "ok",
      "redis": "ok"
    },
    "timestamp": "2026-08-30T10:00:00Z"
  }
}
```

**响应（异常）：** HTTP 503

---

## 10. 数据库设计勘误回填

> 本章记录在接口设计过程中发现的数据库设计说明书（`05_数据库设计说明书.md`）中需要补充或修正的内容。

### 10.1 `sms_codes` 表补充 `purpose` 枚举值

**原设计：** `purpose` 可选值为 `REGISTER / LOGIN / TWO_FA`
**补充：** 新增 `BIND_PHONE`（家长首次绑定手机号用途，区别于 `REGISTER`）

```sql
-- 补充枚举值
-- purpose: REGISTER / LOGIN / TWO_FA / BIND_PHONE
```

### 10.2 `test_sessions` 表补充 `total_time_sec` 计算时机

**说明：** `total_time_sec` 在 `POST /api/v1/test/complete` 接口调用时从 `test_answer_records` 聚合计算并写入，表设计已正确，补充注释：

```sql
total_time_sec  INT,  -- 完成测试时写入，= sum(test_answer_records.time_spent_sec)
```

### 10.3 `training_sessions` 表补充 `is_early_end` 字段

**场景：** 学生提前结束训练（不做完5题），需要区分"正常完成"和"提前结束"，生成不同类型的训练小结。

```sql
-- 在 training_sessions 表中新增
is_early_end    BOOLEAN     NOT NULL DEFAULT FALSE,  -- 是否提前结束训练
```

### 10.4 `wrong_answer_records` 表补充 `review_summary` 长度注释

**补充注释：**
```sql
review_summary  TEXT,  -- AI生成的复盘摘要（≤100字），wrong_review 会话结束时写入
```

### 10.5 `cognitive_test_questions` 表补充 `correct_option` 可空说明

**说明：** `OPEN` 类型题目无标准答案，`correct_option` 为 NULL，接口层不返回此字段。

```sql
correct_option  VARCHAR(5),  -- 仅 STANDARD 类型题目有效；OPEN 类型为 NULL
```

### 10.6 统一分页方式说明

接口设计统一使用 **Keyset Pagination（游标分页）**，`05_数据库设计说明书.md` 所有高频列表查询索引已按此优化（包含时间字段的倒序索引），无需修改，补充设计原因说明：

> 使用 `before_id` + `LIMIT` 代替 `OFFSET` + `LIMIT`，避免大数据量下 OFFSET 性能退化，且支持实时数据一致性。

---

*版本历史：*

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0.0 | 2026-08-30 | 初稿，覆盖小程序端和后台管理端全部功能点，含统一响应格式、错误码体系、认证规范，共9大模块约60个接口，含数据库勘误回填 |
| v1.2.0 | 2026-09-12 | ①五力测试题维护（9.3节）新增完整 CRUD：`POST /admin/cognitive-questions`、`PATCH /{id}/status`（发布/下架）、`DELETE /{id}`，`is_active` 替换为 `status` 三态枚举；②学生管理（9.8节）重构为"用户管理"，新增家长列表、管理员账号列表/创建/启停/重置密码、管理员绑定/解绑接口；③小程序端新增家长主动绑定（2.7b）、学生确认绑定（2.7c）接口；④`bind_method` 新增 `PARENT_SELF` 值，`parent_student_bindings` 新增 `bind_status` 字段 |
| v1.3.0 | 2026-09-12 | 五力测试题维护（9.3节）答案结构重构：①新增/编辑接口移除 `target_power`、`power_weight`、`option_scores` 字段；②答案改用统一 `answers` 数组（每条含 `text` + `force_weights`），选项编号 `key` 由后端自动生成；③题型枚举改为 `OBJECTIVE`/`PREFERENCE`；④列表接口移除 `target_power` 筛选参数；⑤编辑接口统一为单一请求体格式（不再区分 STANDARD/OPEN 两套字段） |
| v1.4.0 | 2026-09-12 | 五力测试题维护（9.3节）继续精简：①移除 `question_type` 和 `correct_option` 字段（五力测试题无唯一答案）；②列表接口移除 `question_type` 筛选参数，新增 `keyword` 搜索参数；③新增/编辑接口新增 `description` 字段（管理员元数据） |
| v1.5.0 | 2026-09-12 | 题库管理接口知识体系树支持 |
| v2.0.0 | 2026-09-13 | 训练模块重构：① 训练会话（5.1）新增 ERROR_QUESTIONS 维度；② 新增维度配置接口（9.4.3~9.4.6）|
| v2.1.0 | 2026-09-13 | 用户管理新增AI助教提示词摘要接口（9.8.11~9.8.14）：① 获取完整摘要（含学习信息+个人洞察+洞察条目历史+Prompt预览）；② 管理员编辑个人洞察摘要；③ 手动新增洞察条目（支持6种类型）；④ 触发学习信息摘要重新生成 |① 训练会话（5.1）新增 ERROR_QUESTIONS 维度，响应新增 `dimension_config` 字段；② 5.5 完成接口新增错题移除逻辑说明；③ 新增错题摘要接口（9.4.6）；④ 9.4 训练参数配置重构：全局参数保留（9.4.1/9.4.2），新增五维度独立配置接口（9.4.3~9.4.5）|：① `GET /admin/knowledge-tree` 接口（9.1.0）新增，返回四级树；② 题目列表接口（9.1.1）新增 `subject_id`、`grade_id`、`chapter_id` 树节点过滤；③ AI分析结果新增 `ai_subject`、`ai_grade`、`ai_chapter`；④ 发布接口（9.1.5）新增归属体系字段 |
| v1.6.0 | 2026-09-13 | 知识体系扩展为五级（科目→年级→学期→单元→知识点）：① 知识树响应新增 `semester` 层级；② 题目列表新增 `semester_id` 参数；③ 分析结果新增 `ai_semester`；④ 发布接口新增 `semester_id`；⑤ 训练维度改用 `semester_id` |
| v1.7.0 | 2026-09-13 | 分析结果新增题目类型与答案字段：① 题目详情（9.1.4）`latest_analysis` 新增 `ai_question_type`（SINGLE_CHOICE/MULTIPLE_CHOICE/FILL_BLANK/TRUE_FALSE/APPLICATION）和 `ai_answer`（结构化答案 JSONB）；② 发布接口（9.1.5）请求体新增 `question_type` 和 `answer` 字段 |
| v1.8.0 | 2026-09-18 | 五力测试题（9.3节）答案权重改制：`force_weights` 由小数制（各项0.0~1.0，合计=1.0）改为整数制（各项0~10非负整数，合计=10），与训练题库 `five_power_weights` 保持一致的分值体系 |（科目→年级→**学期**→单元→知识点）：① `GET /admin/knowledge-tree` 响应新增 `semester` 层级节点；② 题目列表（9.1.1）新增 `semester_id` 筛选参数，响应新增 `semester` 字段；③ 题目详情（9.1.4）`latest_analysis` 新增 `ai_semester`；④ 发布接口（9.1.5）新增 `semester_id`；⑤ 训练会话（5.1）SEMESTER维度改用 `semester_id` 参数 |①新增 `GET /admin/knowledge-tree` 接口（9.1.0），返回科目→年级→章节→知识点四级树，附带各节点已发布题目计数；②题目列表接口（9.1.1）新增 `subject_id`、`grade_id`、`chapter_id` 树节点过滤参数，响应体新增 `subject`、`grade`、`chapter` 归属信息；③题目详情接口（9.1.4）`latest_analysis` 新增 `ai_subject`、`ai_grade`、`ai_chapter` 字段；④发布接口（9.1.5）请求体新增 `subject_id`、`grade_id`、`chapter_id` 归属体系字段 |
