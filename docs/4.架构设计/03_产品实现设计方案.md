# 产品落地实现方案

**文档编号：** PRD-IMPL-V1.2
**版本：** v1.4.0
**日期：** 2026-08-30
**文档性质：** 端到端产品落地总览（整合所有需求文档，作为开发团队的最终参考）
**关联文档：** 所有 `3.需求分析/` 及 `1.参考文档/02_系统交互协议规范.md`

---

## 目录

1. [产品一句话定义与核心假设](#1-产品一句话定义与核心假设)
2. [V1.0 交付范围与最小可行版本](#2-v10-交付范围与最小可行版本)
3. [完整用户旅程（端到端）](#3-完整用户旅程端到端)
4. [用户旅程断点与修复方案](#4-用户旅程断点与修复方案)
5. [模块依赖关系与启动顺序](#5-模块依赖关系与启动顺序)
6. [题库冷启动方案（V1.0最大风险）](#6-题库冷启动方案v10最大风险)
7. [五力算法交付要求](#7-五力算法交付要求)
8. [数据架构与关键表关系](#8-数据架构与关键表关系)
9. [AI调用成本预估与控制](#9-ai调用成本预估与控制)
10. [V1.0交付里程碑](#10-v10交付里程碑)
11. [已知风险与应对预案](#11-已知风险与应对预案)
12. [分析发现的核心问题与处置状态](#12-分析发现的核心问题与处置状态)

---

## 1. 产品一句话定义与核心假设

### 1.1 产品定义

> **MESH 是一款面向初中至高中生的数理化 AI 认知训练平台，通过五力认知诊断驱动个性化学习，让每位学生获得了解自己思维短板的专属 AI 老师。**

### 1.2 核心假设（产品成立的前提）

| 假设 | 描述 | 风险等级 |
|------|------|----------|
| **假设 H-01** | 中学生数理化学习困难的根本原因是认知能力薄弱，而非知识积累不足 | 🟡 中（需真实数据验证） |
| **假设 H-02** | 五力模型（洞察/建构/推演/调适/迁移）能有效区分不同学生的认知差异 | 🟠 中高（需算法科学性论证） |
| **假设 H-03** | 学生愿意用文字与 AI 对话解题，而不仅仅是看答案 | 🟡 中（需 UX 测试验证） |
| **假设 H-04** | 五力画像驱动的个性化选题，比随机选题有显著更好的学习效果 | 🟠 中高（需 A/B 测试） |

### 1.3 与竞品的核心差异

| 维度 | 普通 AI 答题工具 | MESH |
|------|----------------|------|
| 核心逻辑 | 学生提问 → AI 给答案 | 学生答错 → AI 改变介入位置和方式 |
| 个性化方式 | 按知识点匹配题目 | 按知识点 × 五力画像 × 优势入口匹配 |
| AI 目标 | 做对这道题 | 训练对应认知能力 |
| 评估标准 | 答题正确率 | 五力成长变化 |

---

## 2. V1.0 交付范围与最小可行版本

### 2.1 V1.0 必须完成（P0）

```
核心验收链路：
注册/登录 → 五力测试（20题）→ 生成五力画像 → 选择知识点训练
→ AI 定向指导（RAG） → 保存训练结果 → 再测对比成长
```

| 功能 | 模块 | 优先级 | 说明 |
|------|------|--------|------|
| 微信授权登录 + 手机号绑定 | 01_登录 | P0 | 无需家长双验证 |
| 五力认知测试 20 题 | 02_五力测试 | P0 | 含测试中断/恢复 |
| 五力画像生成（三层输出） | 02_五力测试 | P0 | ability/preference/final |
| 雷达图展示 + 文字解读 | 02_五力测试 | P0 | AI 异步生成文字分析 |
| 知识点四级导航（学科→年级→章节→知识点） | 03_训练 | P0 | 需题库支持 |
| 五力权重选题算法 | 03_训练 | P0 | 依赖五力画像 |
| 训练题目展示 + 答题交互 | 03_训练 | P0 | 含答对/答错判定 |
| RAG 解题思路生成（答错后） | 03_训练 | P0 | 双路召回 + 降级 |
| 训练结果保存 + 训练画像更新 | 03_训练 | P0 | 异步处理 |
| AI 助教自由提问（文字对话） | 04_助教 | P0 | 含启发策略 |
| AI 主动介入（连续答错≥2次） | 04_助教 | P0 | 与 03 协同 |
| 错题集管理 | 03+04 | P0 | 跨模块状态机 |
| 五力成长对比（再测） | 02_五力测试 | P0 | 双色雷达图叠加 |
| 管理后台：题库管理 | 后台 | P0 | 题目录入、五力标注审核 |
| 管理后台：数据看板 | 后台 | P0 | 基础数据展示 |

### 2.2 V1.0 不做（延后至 V1.1 或 V2.0）

| 功能 | 原因 |
|------|------|
| 家长端查看报告 | P1，不阻断核心链路 |
| 教师端班级学情 | P2，V1.1 引入 |
| OCR 拍照上传题目 | P1，V1.1 引入 |
| 策略有效性画像对学生展示（学习风格） | V1.1 |
| 每日推送与时长提醒 | V1.1 |
| 高中选修模块题库 | 暂不建设 |

### 2.3 V1.0 目标种子用户

- **目标：** 首批 500 名真实学生用户完成完整学习闭环
- **优先覆盖：** 初三数学 + 高一数学（其余学科在题库建设后开放）
- **验证目标：** 五力画像能否有效区分不同学生的认知差异

---

## 3. 完整用户旅程（端到端）

### 3.1 用户旅程全景

```
Phase 1：入门
┌──────────────────────────────────────────────────────────┐
│  打开小程序                                               │
│    ↓                                                     │
│  [首次] 微信授权 → 手机号验证 → 账号创建                  │
│  [再次] 静默登录（wx.login + Token 校验）                 │
│    ↓                                                     │
│  检查：是否有 five_power_profiles 记录？                   │
│    ├── 无 → 全屏引导页，强引导完成五力测试               │
│    └── 有 → 进入首页（Tab 1 默认选中）                   │
└──────────────────────────────────────────────────────────┘

Phase 2：诊断（首次必经，重测可选）
┌──────────────────────────────────────────────────────────┐
│  五力认知测试（20 题）                                    │
│    ↓                                                     │
│  计算三层输出：                                           │
│    ability_score（能力分）= 标准答案题得分                │
│    preference_score（入口分）= 选项权重累加归一化          │
│    final_score（综合分）= ability×70% + preference×30%   │
│    ↓                                                     │
│  生成五力画像（AI 异步文字解读）                           │
│    ↓                                                     │
│  展示雷达图 + 学习模式推荐卡：                            │
│    有薄弱力：推荐 TRAIN_WEAKNESS（补足你的力）              │
│    均衡型：推荐 LEARN_BY_STRENGTH（一通百通）              │
│    ↓                                                     │
│  [开始个性化训练] 或 [先和 AI 聊聊]                       │
└──────────────────────────────────────────────────────────┘

Phase 3：学习（高频，每日）
┌──────────────────────────────────────────────────────────┐
│  首页（三 Tab）                                           │
│  ┌────────┬──────────┬──────────┐                        │
│  │Tab 1   │Tab 2     │Tab 3     │                        │
│  │个性化训练│AI 助教   │我的五力  │                        │
│  └────────┴──────────┴──────────┘                        │
│                                                          │
│  [路径A：Tab 1 个性化训练]                                │
│    选择知识点 → 生成 5 题专属题卷                         │
│    逐题作答：                                             │
│      答对 → 沉默，进入下题                               │
│      答错 → RAG 生成解题思路（≤8s，超时降级）             │
│           → 连续答错≥2次 + 满足5条件 → 04模块悬浮介入    │
│    训练结束 → 生成小结 → 异步更新五力训练画像              │
│                                                          │
│  [路径B：Tab 2 AI 助教]                                  │
│    自由提问 → AI 多维启发（9种策略）                      │
│    错题复盘 → 苏格拉底式对话                              │
│    产生理解突破 → 成就感反馈                              │
│    AI 推荐实战检验 → 微训练（2~3题）浮层                  │
└──────────────────────────────────────────────────────────┘

Phase 4：成长闭环
┌──────────────────────────────────────────────────────────┐
│  Tab 3 我的五力                                           │
│    查看五力历史成长曲线                                   │
│    选择两次测试对比 → 双色叠加雷达图                      │
│    查看各力详细分析 + 训练建议                            │
│    ↓                                                     │
│  重测五力测试（间隔≥30天推荐）                            │
│    生成新画像 → 自动与历史对比 → 展示成长                 │
└──────────────────────────────────────────────────────────┘
```

### 3.2 跨模块数据流

```
[五力测试完成]
      │ 写入 five_power_profiles（is_latest=true）
      ↓
[个性化训练开始]
      │ 读取 five_power_profiles + five_power_training_profiles
      │ 计算五力权重 → 生成题卷 → 写入 training_sessions
      ↓
[答题过程]
      │ 写入 training_answer_records（每题）
      │ 答错 → 异步写入 wrong_answer_records（review_status=unreview）
      │ 答错 → 异步触发 RAG（Queue-C）
      │ 连续答错≥2 → 触发 ai_chat_sessions 创建
      ↓
[训练结束]
      │ 异步更新 five_power_training_profiles（衰减系数0.30）
      │ 生成训练小结
      ↓
[AI 助教对话]
      │ 读取 five_power_profiles + wrong_answer_records
      │ 写入 ai_chat_messages
      │ 产生理解突破 → 更新 wrong_answer_records（review_status=aha_achieved）
      │ 异步更新 ai_student_strategy_profiles（策略有效性）
      ↓
[五力画像综合查询]
      │ training_score × 0.6 + test_score × 0.4 = 综合五力得分
      └─ 用于下次选题权重计算
```

---

## 4. 用户旅程断点与修复方案

### 4.1 断点 D-01：新用户跳过五力测试

**场景：** 新用户点击"稍后再说"后进入首页，没有五力画像，无法使用 Tab 1。

**修复方案：**

```
进入 Tab 1（无五力画像时）：
  展示引导占位页，而非空白或报错
  ┌────────────────────────────────────────────┐
  │                                            │
  │     🧠  先了解一下你的学习方式              │
  │                                            │
  │  完成 5 分钟五力测试，AI 才能为你           │
  │  生成专属训练方案                           │
  │                                            │
  │        [开始五力测试]                      │
  │                                            │
  └────────────────────────────────────────────┘
```

**规格补充（补充至 02_五力认知测试模块 §7.1）：**
- 无五力画像时，Tab 1 显示引导占位页（不展示知识点选择器）
- 顶部五力状态卡在无数据时显示"完成测试解锁" + 测试入口

---

### 4.2 断点 D-03：知识点选择器中可用/不可用的区分

**场景：** 学生选择某知识点，但该知识点可用题目不足 5 道（未达到开放门槛）。

**修复方案：**

```
知识点选择器 UI 规格：
  ✅ 可用知识点（题目≥5道）：正常显示，可点击
  ⚠️ 建设中知识点（题目1~4道）：灰色显示，点击弹出提示
     "该知识点题库建设中（已有X道），预计XX日开放"
  ❌ 未建知识点（题目=0道）：隐藏（不显示）

数据接口：
  GET /api/knowledge-points?subject=math&grade=g9
  返回字段新增：{ available: true, question_count: 12 }
```

**规格补充（补充至 03_个性化AI训练模块 §7.1）：**
- 知识点选择器接口返回 `available` 和 `question_count` 字段
- 前端按可用性分类展示

---

### 4.3 断点 D-04：RAG 答错展示的降级体验

**场景：** 学生答错，RAG 超时（>8s），从"AI 正在思考…"突然切换为"展示静态解析"，心理落差大。

**修复方案（完整降级流程）：**

```
T=0：学生提交答案
T=0 + 200ms：前端展示 "❌ 回答错误"，同时启动骨架屏
  骨架屏文案："AI 正在从你的薄弱维度分析解题思路…"

T=3s：骨架屏加上进度条（视觉告知正在处理）

T=8s：如 RAG 未返回：
  ├─ 后台触发降级
  ├─ 查询 questions.solution 字段
  ├─ 如 solution 存在：
  │   展示参考解析，角落标注"⚡ 参考解析"（区分于 AI 回复样式）
  │   文案："AI 助手暂时繁忙，这是题目参考解析供参考"
  └─ 如 solution 不存在：
      展示"AI 助手暂时繁忙，建议查看教材对应章节"
      提供[重新生成]按钮（3次机会）

T=15s：RAG 若仍未返回，彻底放弃，清除骨架屏

视觉区分：
  AI 实时回复 → 蓝色背景，"🤖 AI 解题思路"标题
  静态降级   → 浅灰背景，"⚡ 参考解析"标题
```

---

### 4.4 断点 D-05：04 模块悬浮介入的交互细节

**场景：** 学生连续答错≥2次，04 模块要主动介入，但目前仅定义"展示悬浮条"，缺乏完整交互规格。

**修复方案：**

```
悬浮条样式：
┌─────────────────────────────────┐
│  💬 AI 助教有话说               │
│  "你在这道题上遇到困难了，我来   │
│   帮你分析一下条件？"           │
│             [展开对话] [稍后]   │
└─────────────────────────────────┘
位置：答题卡下方，不遮挡题目内容

[展开对话]：
  → 在当前页面弹出内嵌浮层（底部抽屉式，占屏幕50%）
  → 浮层内是 AI 助教对话界面
  → 可以一边查看题目一边对话
  → [收起]按钮折叠浮层，回到纯做题视图

[稍后]：
  → 悬浮条收起，设置 silence_mode=true（当前知识点）
  → 在训练结束小结页提醒："AI 有为你准备解析，查看错题集"

用户主动召唤：
  → 任何时候点击右下角"💬"按钮 → 打开 Tab 2 AI 助教
  → 或长按答题界面 → 弹出"向 AI 提问"快捷入口
```

---

### 4.5 断点 D-06：跨 Tab 切换的训练状态保持

**场景：** 学生在 Tab 1 训练到第 3 题，切换到 Tab 2 聊天，再切回 Tab 1，训练会话是否还在？

**修复方案：**

```
训练会话持久化规则：
  1. training_session 存储于服务端，状态 = active
  2. 切换 Tab 不影响会话，前端重新渲染时恢复当前题目
  3. 退出小程序后：
     ├─ 如已答题但未完成：training_session 保持 active，保留 72h
     └─ 重新进入时：提示"你上次训练进行到第 X 题，继续吗？"

前端实现要求：
  - Tab 1 的组件状态使用全局状态管理（Vuex/Pinia）
  - 切 Tab 时不销毁 Tab 1 组件，仅隐藏
  - 页面路由使用 keep-alive 保留答题进度
```

---

### 4.6 断点 D-08：五力训练画像异步更新的用户感知

**场景：** 学生完成训练，五力画像异步更新，但下次训练用的还是旧画像（5分钟内）。

**修复方案：**

```
前端提示规范：
  训练结束时，在小结页展示：
  "🔄 你的五力数据正在更新，通常在 5 分钟内完成"

  更新完成后，推送微信模板消息（可选）：
  "你的五力数据已更新，查看最新画像 →"

  若学生在 5 分钟内开始新训练：
  ├─ 使用旧画像（可接受，数据更新频率本就不是实时的）
  └─ 在训练入口提示"正在使用上次的能力数据"（小字）

SLA 要求（补充至性能约束）：
  五力训练画像异步更新延迟：目标 ≤ 5 分钟（P95）
```

---

### 4.7 断点 D-10：学生提前结束训练

**场景：** 学生只想做 3 题，但系统固定为 5 题，没有"提前结束"机制。

**修复方案（补充至 03 模块 §7.3）：**

```
训练界面新增"结束训练"按钮（右上角）：
  点击后弹出确认：
  ┌────────────────────────────────────┐
  │  确认结束训练？                     │
  │  当前进度：3/5 题（已完成 3 题）   │
  │  已答题目的数据将保存               │
  │                                    │
  │    [确认结束]  [继续做题]           │
  └────────────────────────────────────┘

提前结束的处理：
  - session_status → completed（标记为提前结束）
  - total_questions 字段写入实际完成题数（3）
  - 生成"训练进度小结"（而非完整小结）
  - 已答题目的数据正常写入 training_answer_records
  - 五力训练画像异步更新（按实际完成题数）
```

---

## 5. 模块依赖关系与启动顺序

### 5.1 技术依赖关系

```
依赖层级（从底层到上层）：

Level 0：基础设施（最先搭建）
  ├─ 微信小程序框架
  ├─ 后端服务（Node.js/Java）
  ├─ 关系型数据库（MySQL/PostgreSQL）
  ├─ 向量数据库（Milvus 或 Chroma）
  └─ 对象存储（图片、PDF 报告）

Level 1：身份认证（其他模块的前提）
  └─ 01_登录模块（JWT 签发、角色权限）

Level 2：核心数据（其他功能的数据基础）
  ├─ 02_五力认知测试模块（生成五力画像）
  └─ 题库建设（独立工程，并行进行）

Level 3：核心业务（依赖 Level 1+2）
  └─ 03_个性化AI训练模块（依赖：登录 + 五力画像 + 题库）

Level 4：增强体验（依赖 Level 3）
  └─ 04_智能助教模块（依赖：登录 + 五力画像 + 错题集）

Level 5：数据沉淀（依赖 Level 3+4）
  └─ 学习记录与反馈（训练数据 + 成长报告）
```

### 5.2 开发启动顺序建议

| 阶段 | 并行工作 | 预计时长 |
|------|----------|----------|
| **Sprint 1** | 01_登录模块（完整） + 数据库表结构设计 | 2周 |
| **Sprint 2** | 02_五力测试模块（完整）+ 题库录入工具 | 3周 |
| **Sprint 3** | 03_AI训练模块（选题+答题+RAG基础版） + 题库初步建设 | 4周 |
| **Sprint 4** | 04_智能助教模块（基础对话+策略切换） + 03模块完善 | 3周 |
| **Sprint 5** | 03+04 模块联调 + 错题集跨模块 + 成长对比 | 2周 |
| **Sprint 6** | 管理后台 + 全链路测试 + 真实学生测试 | 2周 |

---

## 6. 题库建设方案

> **当前策略：** 研发阶段使用 AI 生成少量样例数据保障开发调试不受阻塞，正式题库在并行建设后于内测前完成。

### 6.0 研发阶段题库策略（开发调试专用）

**核心原则：** 研发阶段不依赖真实题库，由 AI 批量生成覆盖所有关键路径的最小样例数据集，保证每个开发 Sprint 的功能可以独立测试与验收。

```
研发阶段 AI 生成题库目标（Dev Seed Data）：

覆盖学科：数学（G9）
覆盖章节：选取 3 个代表性知识点（覆盖不同难度和五力组合）
每知识点：10 道题（满足 5 道开放门槛，预留测试替换空间）
五力覆盖：每知识点每种五力各 2 道题（INSIGHT/STRUCTURE/INFERENCE/ADAPTATION/TRANSFER）

合计：3 个知识点 × 10 题 = 30 道样例题
```

**推荐研发种子知识点（3个）：**

| 知识点 | 编码 | 覆盖场景 |
|--------|------|----------|
| 二元一次方程组应用 | MATH-G9-CH03-KP007 | 洞察/建构/调适力题型丰富，适合测试 RAG 双路召回 |
| 一次函数图像与性质 | MATH-G9-CH05-KP003 | 含迁移力/推演力题型，适合测试一通百通模式 |
| 相似三角形判定 | MATH-G9-CH08-KP002 | 含空间想象/逻辑推演，适合测试 04 智能助教对话 |

**AI 生成题目的质量要求：**

```
每道题必须包含以下字段（与正式题库规范一致）：
  stem          - 题干（支持文字描述，不要求 LaTeX）
  question_type - 题型（计算题/应用题/选择题/判断题）
  solution      - 参考解析（用于 RAG 降级展示）
  primary_power - 主训练五力（5种之一）
  difficulty    - 难度（基础/进阶/挑战）
  common_error  - 典型错误描述（用于 RAG Prompt 注入）
  transfer_direction - 可迁移情境列表（JSON数组）

生成标准：
  ① 题目逻辑自洽，无数学/物理错误
  ② 每道题的 solution 字段必须完整（用于降级兜底）
  ③ common_error 必须真实反映中学生典型错误模式
  ④ 五力标注与题目内容强相关（不可随意打标签）
  ⑤ 不追求题目的教学质量，追求覆盖系统所有代码路径
```

**生成工具与流程：**

```
Step 1：使用大模型（通义千问/Claude）按以下 Prompt 批量生成
  Prompt模板：
  "你是初中数学出题专家，请为以下知识点生成一道{五力类型}训练题：
   知识点：{knowledge_point}，难度：{difficulty}
   题目需包含：题干、完整解析、典型错误、2个可迁移情境
   输出 JSON 格式，字段名参考附件规范"

Step 2：人工快速校验（每题 1~2 分钟）
  - 确认数学逻辑无误
  - 确认五力标注合理
  - 无需深度教学质量审核

Step 3：通过管理后台题库工具导入（JSON 批量导入）
  - annotation_status 直接设为 confirmed（跳过 AI 标注智能体）
  - embedding_status 触发向量化流程

Step 4：完成 30 道种子题入库后，开发团队即可进行全链路联调
```

**研发题库与正式题库的隔离：**

```
环境区分：
  dev 环境  - 使用 AI 生成种子题（30道），允许随意增删改
  staging   - 使用正式题库早期版本（≥200道）
  production - 使用完整正式题库（≥1280道）

数据标记：
  questions.is_seed_data = true（种子数据标记，上线前清除）
  种子数据在生产环境部署前全部删除，不污染正式数据
```

---

### 6.1 V1.0 正式题库目标规模

```
优先覆盖（Phase 1）：
  学科：数学
  年级：初三（G9）+ 高一（G10）
  章节：每年级覆盖 10 个核心章节
  知识点：每章节 5~8 个知识点

题目要求：
  每个知识点：8~10道题（超过5道开放门槛，预留替换空间）
  五力覆盖：每个知识点各力各≥2道题（满足选题算法）

正式题库目标规模：
  G9（初三）数学：80 个知识点 × 8 题 = 640 题
  G10（高一）数学：80 个知识点 × 8 题 = 640 题
  合计：~1280 道数学题（Phase 1 交付目标）
```

### 6.2 题库建设方案（三阶段并行）

| 阶段 | 时间 | 内容 | 目标 |
|------|------|------|------|
| **Phase 0：研发种子数据** | Sprint 1~3 | AI 生成 30 道种子题，人工快速校验 | 解除开发阻塞，覆盖全链路调试 |
| **Phase 1：核心题库建设** | Sprint 3~5 | 人工录入 + AI 辅助生成 + AI 自动标注审核 | G9 数学 640 道，开放内测 |
| **Phase 2：题库扩充** | Sprint 5~6 | AI 辅助生成扩充 + 可选采购合规题库 | G10 数学 640 道，正式上线 |

**各方案对比：**

| 方案 | 优点 | 缺点 | 适用阶段 |
|------|------|------|----------|
| **AI 生成（种子）** | 极速，0成本，30分钟出30道 | 教学质量一般，仅供开发调试 | Phase 0（研发阶段） |
| **人工录入** | 质量可控，五力标注准确 | 速度慢，人力成本高 | Phase 1 核心知识点 |
| **AI 辅助生成 + 人工审核** | 速度快，质量有保障 | 需人工校验流程 | Phase 1+2 扩充 |
| **合规题库采购** | 题目质量高，量大 | 版权成本，需额外五力标注 | Phase 2 可选 |

### 6.3 题库建设里程碑

| 里程碑 | 内容 | 时间节点 | 状态 |
|--------|------|----------|------|
| **M-00** | **AI 生成 30 道研发种子题，导入 dev 环境** | **Sprint 1 第 1 周** | **✅ 研发阶段立即执行** |
| M-01 | 确定正式题库来源方案与版权协议 | 开发启动前（并行推进） | 待确认 |
| M-02 | 完成题库录入工具（后台管理） | Sprint 2 完成 | 开发中 |
| M-03 | 完成 G9 数学 400 道初步正式题库 | Sprint 3 中期 | 待建设 |
| M-04 | 完成 G9+G10 数学 1200 道正式题库 | Sprint 5 前 | 待建设 |
| M-05 | 正式题库 AI 标注覆盖率≥90%，人工审核完成 | Sprint 6 前 | 待建设 |

### 6.4 题库不足时的前端处理

```
知识点选择器状态：
  ✅ 可用（题目≥5道，五力覆盖率≥80%）：正常绿色
  ⚠️ 建设中（题目1~4道）：浅灰色 + "建设中"标签 + 题目数量提示
  🔒 未建（题目=0道）：隐藏（不展示在选择器中）

Phase 1 期间：
  物理、化学知识点全部隐藏（未建）
  数学仅展示 G9+G10 的已建知识点
  其余年级知识点以"灰色+即将开放"展示
```

---

## 7. 五力算法设计说明

> **✅ 当前状态（已完成）：** 五力评分算法已由研发团队完成自主设计，详见 `4.架构设计/02_五力评分算法设计方案.md`。算法覆盖 ability_score / preference_score / final_score 三轨计算、动态更新、选题权重生成和画像置信度评估。算法参数全部配置化，上线后可通过后台热更新，无需改代码。

### 7.1 算法核心参数（已定，后台可调）

| 参数 | 默认值 | 说明 | 来源 |
|------|--------|------|------|
| `objective_weight` | 0.70 | ability_score 在综合分中的权重 | 算法方案§Module C |
| `preference_weight` | 0.30 | preference_score 在综合分中的权重 | 算法方案§Module C |
| `focus_threshold` | 50 | 重点强化项阈值（ability<50=困难） | 02_测试规格 |
| `secondary_threshold` | 65 | 次重点项阈值（50~65=需改进） | 02_测试规格 / 系统协议§7.1 |
| `time_bonus_coefficient` | 0.10 | 时长加分系数（最高加10%） | 算法方案§A2 |
| `history_decay_coefficient` | 0.30 | 重测时历史结果权重（新结果权重=70%） | 算法方案§A3 |
| `weak_boost_level1` | 0.40 | ability<50分力的选题额外权重加成 | 算法方案§Module F |
| `weak_boost_level2` | 0.20 | ability 50~65分力的选题额外权重加成 | 算法方案§Module F |
| `training_profile_decay` | 0.30 | 训练画像更新新数据权重（历史权重=70%）| 算法方案§Module E |
| `test_train_mix_train` | 0.60 | 综合查询：训练画像权重 | 系统协议§6.1 |
| `test_train_mix_test` | 0.40 | 综合查询：测试画像权重 | 系统协议§6.1 |
| `rag_topk_solution` | 3 | 相似题解析检索数 | 03_训练规格 |
| `rag_topk_strategy` | 2 | 五力策略文档检索数 | 03_训练规格 |

### 7.2 仍需客户确认的内容（已从算法阻塞降为产品优化项）

| 确认事项 | 说明 | 优先级 | 影响 |
|----------|------|--------|------|
| 20道测试题完整题面文本 | 当前算法已完成，但题目内容仍需客户提供正式题面 | P0 | 阻断五力测试上线 |
| 选项权重向量专家校验 | 各题各选项的五力权重建议经教育学专家审核 | P1 | 影响画像准确性 |
| 五力画像解读文案模板 | AI个性化分析段落的基础文案，每种五力组合一套 | P2 | 影响用户体验 |

---

## 8. 数据架构与关键表关系

### 8.1 核心数据表 ER 关系

```
students（学生账号）
  │
  ├── five_power_profiles（测试五力画像，每次测试一条）
  │     └── is_latest=true 的为当前有效画像
  │
  ├── five_power_training_profiles（训练五力画像，滚动更新）
  │     └── 与 five_power_profiles 独立，综合查询时 0.6/0.4 融合
  │
  ├── training_sessions（训练会话，每次训练一条）
  │     ├── training_focus_power（本次主训练力）
  │     ├── training_mode（TRAIN_WEAKNESS / LEARN_BY_STRENGTH）
  │     ├── preferred_force（优势入口力）
  │     └── five_power_snapshot（训练开始时的五力快照）
  │           └── training_answer_records（每题答题记录，一对多）
  │
  ├── wrong_answer_records（错题记录，跨模块共享）
  │     ├── source_module（training / test）
  │     └── review_status（unreview → reviewing → aha_achieved）
  │
  └── ai_chat_sessions（AI对话会话）
        ├── session_mode（free_chat / wrong_review / training_error_guidance）
        ├── linked_question_id（关联题目）
        ├── parent_training_session_id（关联训练会话）
        └── ai_chat_messages（对话消息，一对多）
              └── strategy_id（使用的启发策略）

ai_student_strategy_profiles（策略有效性画像，每学生每策略一条）
  └── effectiveness_score（有效性分数，动态更新）
```

### 8.2 关键数据流的事务性要求

| 数据操作 | 事务要求 | 说明 |
|----------|----------|------|
| 学生答题 → 写 training_answer_records | 同步，事务内 | 核心答题记录不允许丢失 |
| 答错 → 写 wrong_answer_records | 异步，事务外（Queue-D） | 允许延迟，失败后重试 |
| 答错 → 触发 RAG | 异步，事务外（Queue-C） | 允许延迟，失败则降级 |
| 训练结束 → 更新 five_power_training_profiles | 异步，目标≤5min | 允许延迟，但必须最终一致 |
| AI对话 → 写 ai_chat_messages | 同步，事务内 | 对话记录不允许丢失 |
| 理解突破 → 更新 wrong_answer_records.review_status | 同步 | 防止并发冲突（行级锁） |

---

## 9. AI调用成本预估与控制

### 9.1 单次学习会话 Token 消耗估算

```
场景：一次标准训练（5题，2题答错，1次AI介入）

03_模块 RAG（每题答错触发）：
  Prompt: ~1300 Token × 2次 = 2600 Token
  Completion: ~400 Token × 2次 = 800 Token
  小计：3400 Token

04_模块 AI对话（1次主动介入，3轮对话）：
  Prompt: ~1050 Token × 3次 = 3150 Token
  Completion: ~800 Token × 3次 = 2400 Token
  小计：5550 Token

单次学习会话总计：~8950 Token（约9K Token）

按 GPT-4o-mini 定价（~0.15$/1M Token in）：
  9000 Token ≈ 0.00135 美元/次会话 ≈ 0.01 元人民币/次会话
  1000次会话/天 = 10元/天 = 300元/月（极低成本）

按通义千问价格（更低）：
  成本更低，基本可忽略不计
```

### 9.2 成本控制措施

| 措施 | 预期节省 |
|------|----------|
| 答对时完全不调用 AI（沉默原则） | 节省 ~60% 的 RAG 调用 |
| 选择题答错使用预置解析（不触发 RAG） | 节省 ~30% 的 RAG 调用 |
| 对话上下文裁剪（10轮→5轮时） | 节省 ~300 Token/次对话 |
| 主动介入次数限制（每会话≤3次） | 限制 04 模块调用上限 |
| 静态降级（RAG超时后展示预置内容） | 避免重试成本 |

---

## 10. V1.0 交付里程碑

### 10.1 里程碑总览

```
M-0（立即，Sprint 1 第1周）：
  ✅ AI 生成 30 道研发种子题，导入 dev 环境（解除开发阻塞）
  ✅ 确认五力算法交付时间 + 正式题库来源方案

M-1（Sprint 2 完成）：
  ✅ 登录模块完整上线
  ✅ 五力测试模块完整（含20道正式题目）
  ✅ 后台题库录入工具就绪（支持 JSON 批量导入）
  ✅ 基础数据架构完成

M-2（Sprint 3 完成）：
  ✅ 个性化训练模块基础版（选题+答题+静态解析）
  ✅ G9 数学初步正式题库（≥400道）
  ✅ AI标注智能体上线

M-3（Sprint 4 完成）：
  ✅ RAG系统集成（向量库+双路召回）
  ✅ 智能助教基础对话（含9种启发策略）
  ✅ G9+G10 数学题库（≥1200道）

M-4（Sprint 5 完成）：
  ✅ 03+04模块联调（悬浮介入+微训练）
  ✅ 错题集跨模块状态机
  ✅ 五力成长对比（双色雷达图）
  ✅ 学习模式路由（TRAIN_WEAKNESS/LEARN_BY_STRENGTH）

M-5（Sprint 6 完成）：
  ✅ 管理后台完整
  ✅ 全链路E2E测试
  ✅ 真实学生内测（目标50名）
  ✅ V1.0上线条件验收
```

### 10.2 V1.0 上线验收标准

```
核心链路验收（必须通过）：
□ 真实学生能完整跑通：
  注册 → 五力测试 → 五力画像生成 → 选择知识点
  → 完成5题训练 → AI生成解题思路（答错时）
  → 保存训练结果 → 再测后对比五力成长
□ 核心链路端到端延迟 ≤ 3秒
□ RAG生成时间 ≤ 8秒（P90），超时后正常降级
□ 五力画像生成时间 ≤ 3秒（同步评分部分）

题库验收：
□ G9数学至少 400 道题，每知识点≥5道
□ AI标注置信度≥0.85题目占比≥80%
□ 所有已发布题目完成向量化入库

性能验收：
□ 并发50用户时，答题接口响应 ≤ 500ms
□ 选题算法执行 ≤ 3秒（P95）

安全验收：
□ 未成年人数据加密存储
□ JWT Token 过期机制正常
□ 管理后台访问控制有效
```

---

## 11. 已知风险与应对预案

| 风险编号 | 风险描述 | 概率 | 影响 | 应对预案 |
|----------|----------|------|------|----------|
| **R-01** | 正式题库未按时建设完成 | 中 | 🟠 影响内测时间 | **研发阶段已用 AI 生成 30 道种子题解除开发阻塞（M-00）**；正式题库 Phase 1 仅开放 G9 数学；提前并行启动题库建设（M-01~M-03）|
| **R-02** | ~~五力算法未交付~~ | — | ✅ **已解除** | 五力评分算法已由研发团队自主完成设计（见 `4.架构设计/02_五力评分算法设计方案.md`），所有参数后台配置化。仅余20道题面文本和权重专家校验需客户提供，不阻断开发 |
| **R-03** | RAG 检索质量不达标（相关性<0.75） | 中 | 🟠 影响体验 | 降级方案已设计（直接调 LLM 不注入检索）；上线前先建高质量策略文档库 |
| **R-04** | AI 对话成本超预算 | 低 | 🟡 可控 | Token 预算已规范化；多重沉默策略已设计；可切换更低价 LLM |
| **R-05** | 五力测试的认知科学有效性未验证 | 中 | 🟠 影响可信度 | V1.0 以"收集数据"为目标，对外不作过度承诺；Sprint 6 后做数据分析 |
| **R-06** | 微信小程序审核被拒 | 低 | 🟡 延期 | 提前 2 周提交审核；内容安全机制已设计；未成年人保护已明确 |
| **R-07** | LLM API 服务不稳定 | 低 | 🟡 影响体验 | 降级方案已设计；支持多供应商切换（配置化） |

---

## 12. 分析发现的核心问题与处置状态

> **说明：** 本章记录通过系统性跨文档分析发现的设计缺陷与缺失项，按严重程度分级，并标注每个问题的处置状态与落地位置，供开发团队作为验收基准使用。

### 12.1 问题总览

| 优先级 | 问题数量 | 处置完成 | 待处置 |
|--------|----------|----------|--------|
| 🔴 P0 阻断 | 3 | **3** | **0** |
| 🟠 P1 影响体验 | 6 | 6 | 0 |
| 🟡 P2 体验优化 | 3 | 3 | 0 |
| **合计** | **12** | **12** | **0** |

> **✅ 所有设计阻断点已清零（2026-08-30）。** 产品可进入开发实施阶段。

---

### 12.2 P0 阻断级问题（必须在上线前解决）

#### 问题 P0-01：题库冷启动——无题库则核心链路无法运转

**问题描述：**
客户文档明确标注"有待解决：题库！"。选题算法要求每个知识点至少 5 道已标注题目才能开放训练入口，但题库数量、来源方案、五力标注方式均未确定。无题库 → 个性化选题算法形同虚设 → V1.0 核心链路无法跑通。

**影响范围：** 03_个性化AI训练模块、V1.0 上线时间

**严重程度：** 🟠 影响内测时间（研发阶段已解除阻塞）

**处置方案（已落地）：**

| 措施 | 内容 | 来源章节 |
|------|------|----------|
| **研发种子数据（新增）** | **Sprint 1 第1周，AI 生成 30 道种子题导入 dev 环境，覆盖 3 个知识点 × 5 种五力，解除开发阻塞** | **§6.0（M-00）** |
| 正式题库规模目标 | G9+G10 数学各 80 知识点 × 8 题 = 合计 ~1280 道 | §6.1 |
| 三阶段建设方案 | Phase 0（种子）→ Phase 1（核心）→ Phase 2（扩充）并行推进 | §6.2 |
| 知识点可用性三态 | ≥5道=可用（绿）、1~4道=建设中（灰）、0道=隐藏 | §4.2（断点D-03修复） |
| 前端接口规格 | 选择器接口新增 `available` 和 `question_count` 字段 | 03模块 C-TR-002a |
| 建设里程碑 | M-00~M-05 明确各阶段题库交付节点 | §6.3 |
| 种子数据隔离 | `is_seed_data=true` 标记，生产部署前清除，不污染正式数据 | §6.0 |

**研发阶段（✅ 已解决）：**
- Sprint 1 第 1 周：AI 生成 30 道种子题，完成 dev 环境全链路联调

**正式题库（✅ 产品决策已定，执行中）：**

> **产品经理决策（2026-08-30）：** 正式题库 V1.0 阶段采用"人工编写"方式，不依赖第三方采购，无版权风险。理由：①人工编写的题目可按五力标注规范精确设计，质量可控；②避免采购谈判周期影响上线节点；③Phase 2 可再评估是否引入外部题库扩充。

- [x] 题库来源确认：**人工编写**（不采购，不使用爬取内容）
- [x] 五力标注方式：**人工标注 + AI辅助建议**（AI给出初始标签，人工审核确认）
- [x] 每知识点最低题数门槛：**5道**（已写入规格 C-TR-002，确认执行）
- [x] WBS 责任人：由研发团队内容组负责，并行于开发推进

---

#### 问题 P0-02：五力评分算法未交付——画像失准则个性化失效

**问题描述：**
需求文档多次提及"五力评分算法由产品方提供"，但当前未见任何算法文档。五力画像的准确性直接决定选题权重和 AI 教学入口的精准度，若算法不科学，整套个性化体系都是错误的起点。

**影响范围：** 02_五力认知测试模块、03_个性化AI训练模块、04_智能助教模块

**严重程度：** ✅ **已解决**（研发团队自主完成算法设计，不再依赖客户交付）

**处置方案（已完成）：**

| 措施 | 内容 | 落地文档 |
|------|------|----------|
| **算法自主设计（已完成）** | **研发团队完成六模块完整算法：ability_score（双轨三步）、preference_score（权重累积归一化）、final_score（加权合成）、画像分析标签、动态更新（EWMA）、选题权重生成（双档加权）** | **`4.架构设计/02_五力评分算法设计方案.md`** |
| 02_测试规格同步更新 | 补充 preference_score 完整计算步骤（Steps B1~B3）；test_results 表补充 ability/preference/final 三套字段 | 02_测试规格 §4.5.1、§5.5 |
| 03_训练规格同步更新 | 选题权重参数拆分为双档（weak_boost_level1/level2）；关闭开放问题 Q-TR-004 | 03_训练规格 §8.5、§13 |
| 全参数配置化 | 所有评分参数后台可调，上线后根据真实数据实时优化 | 算法方案§11 |

**仍需客户提供（已降级为产品优化项，不阻断开发）：**
- [ ] 20道测试题完整题面文本（算法已就绪，等题目内容）
- [ ] 选项权重向量专家校验（影响画像准确性，不影响系统运行）

---

#### 问题 P0-03：用户旅程存在 10 处断点——全链路有缺口

**问题描述：**
通过系统性梳理从注册到学习闭环的完整旅程，发现 10 处交互断点，导致用户体验割裂或数据丢失。

**影响范围：** 全模块

**严重程度：** ✅ **已全部解决**（10处断点均已落地修复方案）

**处置方案（全部已落地，见 §4）：**

| 断点编号 | 断点位置 | 处置方案摘要 | 落地位置 |
|----------|----------|-------------|----------|
| D-01 | 新用户跳过五力测试 | Tab 1 无画像时展示引导占位页，而非空白 | §4.1 |
| D-02 | 五力测试→首页路由 | 画像写入DB后立即更新前端状态卡 | §3.1 |
| D-03 | 知识点选择器无可用性区分 | 三态展示（可用/建设中/隐藏）+ 接口字段 | §4.2 |
| D-04 | RAG 降级体验割裂 | 完整降级时序（T=0→T=8s），视觉区分样式 | §4.3 |
| D-05 | 04模块悬浮介入交互缺失 | 底部抽屉浮层规格 + [暂时跳过]逻辑 | §4.4 |
| D-06 | 跨 Tab 切换训练丢失 | keep-alive 保留 Tab 1 状态，服务端 session 持久化 | §4.5 |
| D-07 | 错题状态权限不清 | 明确：03只写 unreview，04负责状态更新 | 系统交互协议 §6.2 |
| D-08 | 训练画像更新延迟无感知 | 小结页提示"更新中"+ SLA ≤5分钟 | §4.6 |
| D-09 | 多设备登录冲突无通知 | 新设备登录推送"账号在其他设备登录"通知 | 含于 §3.1 |
| D-10 | 无提前结束训练机制 | 新增"结束训练"按钮 + 训练进度小结类型 | §4.7 + 03模块 C-TR-002b |

---

### 12.3 P1 体验影响级问题（V1.0 上线前解决）

#### 问题 P1-01：策略有效性画像数据模型与更新规则缺失

**问题描述：**
04_智能助教模块多次提及"记录每位学生对各种启发方式的响应"，但数据结构、评分算法、查询规则、冷启动处理完全缺失。若不解决，AI 无法实现真正的策略个性化，每次都会套用固定策略顺序。

**影响范围：** 04_智能助教模块

**处置方案（已落地至04模块 §4.5.2~4.5.5）：**

| 补充内容 | 规格 |
|----------|------|
| 初始值 | 新用户所有策略 ES = 50（中性初始，防止冷启动偏差） |
| 评分公式 | ES = (effective_count + positive_ratings × 0.5) / (total_uses + 1) × 100 |
| 等级映射 | ES≥70=高效（优先），40~70=一般，<40=低效（降权不排除） |
| 查询算法 | 5步查询：排除已用策略 → 按ES降序 → 数据充足走历史 / 不足走70%历史+30%默认 |
| 更新SLA | 会话结束后 ≤ 60 秒内异步完成 |
| 同一策略多次计数 | 只记最终结果（突破=有效，被切换=无效），不重复累计 |

---

#### 问题 P1-02：RAG 降级 UX 粗糙——超时切换无过渡

**问题描述：**
原规格仅描述"展示静态解析"，未定义前端动画、视觉区分、等待时序、重试机制，导致学生从"AI 正在思考"突然切换为静态内容，心理落差大。

**影响范围：** 03_个性化AI训练模块、学生答错后的关键体验时刻

**处置方案（已落地至03模块 §6.6）：**

| 补充内容 | 规格 |
|----------|------|
| 完整时序 | T=0：骨架屏；T=3s：进度条；T=8s：降级；T=15s：彻底放弃 |
| 视觉区分 | AI实时回复=蓝色背景"🤖"，静态降级=浅灰背景"⚡" |
| 无解析时兜底 | 展示"建议查看教材对应章节" + [重新生成]按钮 |
| 重试机制 | 每题最多3次重试，等待时限逐次提升（8s→12s→15s） |
| solution 不存在时 | 明确降级链路，不出现空白 |

---

#### 问题 P1-03：微训练调度交互流程空白

**问题描述：**
交互协议定义了"04模块可调度03模块出2~3题"，但从 AI 判断需要实战检验，到 UI 弹出题目、学生做题、结果反馈给 AI 的完整交互流程，在任何文档中均无规格。

**影响范围：** 04_智能助教模块 × 03_个性化AI训练模块

**处置方案（已落地至04模块 §4.5b）：**

| 补充内容 | 规格 |
|----------|------|
| 触发信号 | 学生"我懂了"但突破信号模糊；话题讨论超5轮未验证；学生主动要求 |
| 浮层样式 | 底部弹出，含知识点+训练重点+[开始做题]/[暂时跳过] |
| 题目来源 | 调用03模块 POST /api/training/mini-session，传入知识点+目标五力 |
| 会话关联 | 创建 training_session（parent_chat_session_id=对话会话ID） |
| 完成后 | 自动关闭浮层，AI获取答题结果继续对话 |
| API规范 | Request/Response 字段已完整定义 |

---

#### 问题 P1-04：沉默模式生命周期不完整

**问题描述：**
文档仅说"学生发送'我自己想'后进入沉默模式，学生发消息后解除"，缺少：沉默范围（当前题/整个会话）、超时自动解除、学生如何主动重新召唤 AI、多设备情况下的同步。

**影响范围：** 04_智能助教模块

**处置方案（已落地至04模块 §4.3.3）：**

| 补充内容 | 规格 |
|----------|------|
| 触发方式 | 关键词匹配 OR 悬浮条[暂时不用]按钮 |
| 沉默范围 | silence_scope = question（默认）或 session |
| 自动解除条件 | 5条：主动发消息/切换到下题/会话结束/持续10分钟超时/学生点击💬 |
| 超时自动解除 | 沉默超10分钟自动解除，防止永久静默 |
| 重新召唤 | 训练界面右下角始终显示💬图标（即使在沉默模式中） |

---

#### 问题 P1-05：知识点选择器无可用性标注规格

**问题描述：**
规格要求"每知识点≥5道才能开放训练入口"，但前端如何区分展示可用/建设中/未建知识点、接口返回哪些字段、用户点击不可用知识点的提示文案，均未定义，开发无法实现。

**影响范围：** 03_个性化AI训练模块 §7（选择器交互）

**处置方案（已落地）：**

| 补充内容 | 规格 |
|----------|------|
| 三态展示 | ✅可用（绿）/ ⚠️建设中1~4题（灰色+题数提示）/ 隐藏（0题） |
| 接口字段 | GET /knowledge-points 返回 `available: bool` + `question_count: int` |
| 规格约束 | 03模块约束表新增 C-TR-002a |

---

#### 问题 P1-06：跨 Tab 切换训练状态丢失

**问题描述：**
学生在 Tab 1 做到第3题，切换到 Tab 2 AI 助教，再切回 Tab 1，训练状态是否保留未定义。若不保留，学生每次切 Tab 都要重新选题，体验极差。

**影响范围：** 前端状态管理、03模块训练会话

**处置方案（已落地至 §4.5）：**

| 补充内容 | 规格 |
|----------|------|
| 前端方案 | Tab 1 组件使用 keep-alive，切 Tab 不销毁，仅隐藏 |
| 服务端方案 | training_session 关联 student_id，允许跨 Tab 恢复 |
| 退出小程序 | 训练进度保留72小时，重进提示"继续上次训练？" |

---

### 12.4 P2 体验优化项（V1.0 内完成，不阻断上线）

#### 问题 P2-01：提前结束训练无机制

**问题描述：** 场景 S-01 描述学生完成全部5题，但无中途退出机制，用户被强制锁定在既定题数中。

**处置方案（已落地至 §4.7 + 03模块 C-TR-002b）：**
新增"结束训练"按钮，允许随时结束；提前结束生成"训练进度小结"（区别于完整小结），数据正常写入。

---

#### 问题 P2-02：五力训练画像异步更新延迟无用户感知

**问题描述：** 训练结束后画像异步更新（目标≤5分钟），但期间学生开新训练仍用旧画像，且无任何提示。

**处置方案（已落地至 §4.6）：**
训练小结页展示"🔄 你的五力数据正在更新"；更新完成后可选推送通知；下次训练入口展示"正在使用上次的能力数据"（小字提示）。

---

#### 问题 P2-03：去重窗口定义模糊（时间窗口 vs 题数窗口）

**问题描述：** 规格说"近N道已答题目去重"，但未明确是"最近N天内"还是"最近N题"，两种实现逻辑完全不同。

**处置方案（已落地至03模块 C-TR-006）：**
明确定义为"跨会话最近 N 条 training_answer_records（按 student_id 查询，不限时间）"，默认30题。

---

### 12.5 问题处置状态汇总

| 问题编号 | 问题简述 | 严重程度 | 处置状态 | 落地文档 |
|----------|----------|----------|----------|----------|
| P0-01 | 题库冷启动 | ✅ 已解决 | 研发种子题30道已解除开发阻塞；正式题库采用**人工编写**方案（PM决策，无外部依赖）；三阶段并行推进 | §6.0~6.3 + §12.6 Q-01决策 |
| P0-02 | 五力算法未交付 | ✅ 已解决 | 研发团队自主完成算法设计；**20道正式题目已写入规格 §4.6**（Q-02a解决）；权重校验V1.1数据驱动 | `4.架构设计/02_五力评分算法设计方案.md` + 02_测试规格 §4.6 |
| P0-03 | 用户旅程10处断点 | ✅ 已解决 | 10处断点全部已有修复方案并落地至规格文档 | §4（D-01~D-10） |
| P1-01 | 策略有效性画像缺失 | 🟠 严重 | ✅ 数据模型+算法已补充 | 04模块 §4.5.2~4.5.5 |
| P1-02 | RAG降级UX粗糙 | 🟠 严重 | ✅ 完整时序+视觉规格已补充 | 03模块 §6.6 |
| P1-03 | 微训练交互空白 | 🟠 严重 | ✅ 完整流程+API已补充 | 04模块 §4.5b |
| P1-04 | 沉默模式生命周期不完整 | 🟠 影响体验 | ✅ 5条解除条件+超时规则已补充 | 04模块 §4.3.3 |
| P1-05 | 知识点选择器无可用性标注 | 🟠 影响体验 | ✅ 三态规格+接口字段已补充 | 03模块 C-TR-002a |
| P1-06 | 跨Tab切换训练丢失 | 🟠 影响体验 | ✅ keep-alive+服务端规格已补充 | §4.5 |
| P2-01 | 提前结束训练无机制 | 🟡 优化 | ✅ 按钮+进度小结类型已补充 | §4.7 + 03模块 C-TR-002b |
| P2-02 | 训练画像更新无感知 | 🟡 优化 | ✅ 提示文案+SLA已补充 | §4.6 |
| P2-03 | 去重窗口定义模糊 | 🟡 优化 | ✅ 明确为"最近N题"跨会话 | 03模块 C-TR-006 |

---

### 12.6 待确认事项处置记录

> **产品经理统一决策（2026-08-30）：** 以下事项均已完成内部决策，不再依赖外部确认，不阻断开发。

| 编号 | 原问题 | 决策结果 | 状态 |
|------|--------|----------|------|
| **Q-01** | 正式题库来源方案与版权协议 | **人工编写，不采购**。无版权风险，人工团队负责 Phase 1（G9数学 640 道），研发内部并行推进 | ✅ 已决策 |
| **Q-02a** | 20道测试题完整题面文本 | **已解决**。题目内容来自《五力认知测试 新版》专家设计稿，已正式写入 `02_五力认知测试模块需求规格说明书.md §4.6`，20道题全部具备完整题面、选项和评分权重 | ✅ 已解决 |
| **Q-02b** | 选项五力权重向量专家校验 | **V1.0 使用当前设计权重直接上线**，通过真实学生数据验证有效性，V1.1 基于数据结果迭代优化权重 | ✅ 已决策 |
| **Q-03** | 每知识点最低题数门槛 | **确认 5 道**（已写入规格 C-TR-002）。低于 5 道的知识点前端显示"建设中"，不阻断系统运行 | ✅ 已决策 |
| **Q-04** | V1.0 上线范围 | **确认以"G9数学"单学科单年级内测**。理由：集中资源快速验证核心链路，降低内容风险，内测反馈良好后逐步扩充 G10 及物化学科 | ✅ 已决策 |
| **Q-05** | 认知科学有效性是否需外部专家评审 | **V1.0 不做外部评审**。产品定位为"数据收集+快速迭代"阶段，通过真实学生学习效果数据验证，Sprint 6 后分析数据，V1.1 基于结果决定是否引入外部评审 | ✅ 已决策 |

**当前状态：所有待确认事项已全部关闭，无遗留外部依赖。**

---

*版本历史：*

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0.0 | 2026-08-29 | 初稿，整合所有需求调研与规格文档，生成端到端产品落地总览 |
| v1.1.0 | 2026-08-30 | 新增第12章：分析发现的12个核心问题及处置状态（3个P0、6个P1、3个P2），并列出5条待客户确认事项 |
| v1.2.0 | 2026-08-30 | 新增 §6.0 研发阶段题库策略：AI生成30道种子题（M-00）解除开发阻塞；§6 重组为三阶段方案；同步更新里程碑、风险表、§12 P0-01 处置状态 |
| v1.3.0 | 2026-08-30 | §7 改为"算法设计说明"（P0-02 已解决）；R-02 风险解除；P0-02 状态更新为✅已解决；Q-02 拆分为 Q-02a/Q-02b；P0阻断数从1降至0；同步更新 §12.5 汇总表 |
| v1.4.0 | 2026-08-30 | **所有阻断点清零**：①20道测试题题面正式写入 02_测试规格 §4.6（解除 Q-02a）；②P0-01题库方案PM决策（人工编写，无外部依赖）；③P0-03用户旅程断点全部解决；④Q-01~Q-05全部关闭；⑤§12.6改为决策记录；§12.1总览更新为12/12 |
